# CLAUDE.md — ptcg-ebay-tool プロジェクト概要

このファイルは Claude Code が作業を始める際の参照ドキュメントです。

---

## プロジェクト概要

日本語版ポケモンカードの情報から **eBay 出品用の英語タイトルと説明文を自動生成する** ローカル Web アプリ。

- フロント: Vite + React (`src/App.jsx`)
- カードデータ: `src/cardData.json`（128 セット、TCGdex のオープンデータから生成）
- カード画像: **ローカルにダウンロードして同梱** (`public/cards/` 配下、`src/imageIndex.json` で参照）

---

## 重要ルール

- **`public/cards/` 配下の画像ファイル（.webp/.png/.jpg）を Read ツールで読み込まないこと**

---

## 現在の状態（2026-08-01時点）

### 出品作成機能（`src/App.jsx`）

- 印刷バリエーション: PMCG1〜6（拡張パック〜ジム拡張2）は 1st Edition マーク有無をタイトル/説明文に反映
- 鑑定品モード: PSA/BGS/CGC/SGC のグレード・鑑定書番号に応じてタイトル/説明文を切替
- 選択カードの eBay 相場（売却済み/現在の出品）へのリンク表示
- 仕入れ値・為替・eBay 手数料（定率＋固定額）から想定利益を計算
- 出品履歴: 出品内容を localStorage に保存し、一覧から再読み込み・削除可能

### 画像カバレッジ

日本語カード画像: **9331 / 9372 枚 (99.6%)**  
jpg=7150, png=2181, webp=0

| シリーズ | 状態 |
|----------|------|
| SV (スカーレット&バイオレット) | ほぼ完了（公式 jpg。SVB/SVD/SVF に計24枚の欠け、公式DBに存在しないエネルギー付属カード）|
| S (ソード&シールド) | ✓ ほぼ完了（公式 jpg。SI/S6/S11 に計11枚の欠け、同名カードの候補数不一致）|
| M (メガ) | ✓ 完了（公式 jpg）|
| PCG (EX era: PCG1-9) | ✓ 完了（pcg-search.com png）|
| neo (neo1-4) | ✓ 完了 |
| E (e series: E1-E5) | ✓ 完了（pcg-search.com png）|
| VS1 | ✓ 完了（特殊エネルギーは vs0en7-9.png）|
| web1 | ✓ 完了（pcg-search.com png）|
| PMCG1-4 | ✓ 完了 |
| PMCG5-6 | ほぼ完了（PMCG5に2枚、PMCG6に1枚の欠け）|
| S-P / SV-P（プロモ） | ✓ 完了（`scrape-promo-images.mjs`、公式キャッシュの位置マッチングで取得。**`npm run images` には未組み込み**、手動実行が必要）|

### 対応履歴（2026-07-13）

このワークツリーの `public/cards`（`.gitignore` 対象）は一時的に約900枚不足していた。以下の対応で 8466枚 → 9331枚まで回収：

1. `npm run images` + `scrape-promo-images.mjs`（未組み込みスクリプト）の再実行で475枚回収
2. `scrape-official-images.mjs` の同名カード判別ロジックを改善（296枚回収）。詳細ページでの番号照合が失敗するケース（同名カードが公式サイト上で同じ号数を共有しているなど）向けに、**候補数とcardData側のlocal数が一致する場合は号数照合をスキップし、番号順・掲載順で1:1に対応付けるフォールバック**を追加。さらに末尾の括弧注記（例:「博士の研究（ナナカマド博士）」）が公式DB側にない場合のフォールバックも追加
3. PMCG1-6でトレーナーカードの意訳・パラフレーズ名を pcg-search.com の実名に修正（計109件）。「わるい」プレフィックスの欠落（PMCG4）、ジムリーダー名の言い回し違い（PMCG5/6、例:「ミスティの決闘」→「カスミの勝負」）、全角/半角表記ゆれ（SV4M/SV10/SV4K）など
4. SV4M/SV10のポリゴン２/ポリゴンZの全角数字・全角アルファベット表記を半角に修正

**試したが不採用**: `scrape-old-images.mjs`（英語版TCGdex CDNから代替画像を取得）。日本語カードの出品に英語アートワークを使うのは実物と異なり不適切なため、ダウンロードした1446枚のwebpは削除済み。

### 対応履歴（2026-08-01）: 外部レビュー指摘への対応

出品事故につながりうる不具合を中心に、外部コードレビューの指摘に対応した。

- **英語名の引き継ぎバグ（最重要）**: `applyCandidate`（`src/App.jsx`）が英語名未登録カードを選んだ際、
  前に選んだカードの英語名を引き継いでいた（`en || p.pokemonEn`）。空のまま入力するよう修正し、
  候補タイルへの「英語名なし」バッジ表示、生成欄への警告表示を追加。データ上、画像ありカードの
  45.1%・Sシリーズの92%で英語名が欠損しているため頻発しうる不具合だった。
- **履歴読み込みのクラッシュ対策**: `DEFAULT_FORM` を定数化し、`loadFromHistory` で
  `{ ...DEFAULT_FORM, ...entry.f }` にマージすることで、フィールド追加前の古い履歴を読んでも
  未定義プロパティで落ちないように修正。壊れた履歴エントリは `loadHistory` でフィルタ。
  `ErrorBoundary` も追加し、想定外のエラーで画面全体が白くならないようにした。
- **英語版アートワーク混入経路の遮断**: `build-image-index.mjs` の `.webp` 収集を削除、
  `package.json` から `scrape`（`scrape-images.mjs`）を削除、`scrape-images.mjs` /
  `scrape-old-images.mjs` は `scripts/deprecated/` に移動。
- **スクレイパ堅牢性**: `scrape-official-images.mjs` の詳細キャッシュがnullを永久保存する不具合を修正、
  画像書き込みをtemp+renameでアトミック化（`scrape-pcg-search.mjs` も同様）、
  ページ取得失敗の記録と一定数超過時の中断、同時実行数/待機時間を `scrape-pcg-search.mjs` と同じ
  `CONCURRENCY=3, DELAY_MS=400` に統一、全fetchに `AbortSignal.timeout` を付与、
  HTMLエンティティのデコード処理を追加（S-Pの4件のTAG TEAMカード名混入を修正）。
- **旧裏（オールドバック）対象の拡大**: `PMCG1-6` のみだった判定を `neo1-4` / `VS1` / `web1` にも拡大
  （`OLD_BACK_SET_RE`）。1st Editionの判定は従来通り `PMCG1-6` のまま。
- **検索の改善**: `normalize` にNFKC正規化（全角英数吸収）・中黒/空白の吸収を追加、
  日本語名・英語名がどちらも無いカード（1,739件）を検索候補から除外。
- **利益計算**: `eBay手数料率` のヒントに国際手数料・為替スプレッド込みの実効レート目安
  （17〜19%）を追記。
- **DP/DPt/BW/ADV/LEGEND/XY/SM世代のデータ補完は見送り**（詳細は下記セクション参照）。

### 対応履歴（2026-08-02）: 発売年データ追加・英語名の言語混入バグ修正

- **セット発売年（`y`フィールド）を追加**: TCGdex API（`/v2/ja/sets/{id}` の `releaseDate`）から
  128セット中81セットの発売年を取得（`scripts/fetch-set-years.mjs`）。残り47セット
  （プロモ・構築済みデッキ等の細分化コード、例: `S-P`/`SV-P`/`SI`/`SDL`/`SVB`等）は
  TCGdexに該当セットが存在せず404のため `y` なし。Item Specifics の
  Year Manufactured / Vintage判定はこの `y` の有無に依存する。**未取得47セット**（プロモ・
  構築済みデッキ・スターターセット等の細分化コード）: `S-P, SC1D, SC1a, SC1b, SC2D, SC2a,
  SC2b, SCA, SCB, SCC, SCD, SDL, SDM, SDP, SH, SI, SJ, SK, SLD, SLL, SN, SP5, SP6, SPD, SPZ,
  CSMPiC, SV-P, SV3s, SV4s, SV5s, SV6s, SV7s, SV8s, SV9s, SVAL, SVAM, SVAW, SVB, SVC, SVD,
  SVDs, SVEL, SVEM, SVF, SVHK, SVHM, SVP1`。TCGdexにセット情報自体が無いため手で埋めるしか
  なく、対応する場合は公式サイト等で発売年を個別に確認して `cardData.json` の該当セットに
  `y` フィールドを追記すること。
- **英語名フィールドへのインドネシア語混入を発見・修正（重要）**: 主にSVシリーズの
  トレーナー/エネルギーカード151件（86種類のユニーク値、`SV1S/SV1V/SV1a/SV2D/SV2P/SV2a/
  SV3s/SV4a/SV4s/SV5s/SV6s/SV7s/SV8a/SV8s/SV9s/SVAL/SVAM/SVAW/SVDs/SVHK/SVHM` 等）で、
  英語名フィールドに英語ではなくインドネシア語の翻訳文が入っていた（例:
  「博士の研究（オーリム博士）」の英語名が `Penelitian Profesor (Profesor Olim)`）。
  一部の構築済みデッキ系セット（SVAL/SVAM/SVAW等）では日本語名と英語名が別カードの
  データになっている（位置ズレ）ケースもあった（例: `SVAL/011` の日本語名は「ロトム」だが
  英語名は無関係なエネルギーカードの訳文）。原因未特定（TCGdex側かこのリポジトリの生成
  過程かは未調査）。**汚染された英語名は空文字列にリセット**（`scripts/
  blank-contaminated-en-names.mjs`）。誤った情報を出品に使うより、A-1で追加した
  「英語名なし」警告で安全に運用する方針。**正しい英語名への復元は別タスクとして未着手**。

### 対応履歴（2026-08-03）: 英語名混入の再調査（単語リスト方式の限界と復元の断念）

外部レビューで「除外リスト方式は原理的にこの問題を解決できない（部分混入・固有名詞は検出不可）」
との指摘を受け、再調査した。結果、指摘は正しく、以下の追加対応を行った。

- **単語リストを大幅拡張し検出漏れを追加で74件発見・空欄化**: 色・数字・日用品・感情表現などの
  一般語彙を追加（"Ursaluna Bulan Merah ex"＝正しい英語のポケモン名＋インドネシア語の混成、
  "Pelajar Paldea"＝固有名詞以外が全てインドネシア語、等）。ランダムサンプリングで検証するたびに
  新たな検出漏れが見つかったため、**単語リスト方式では100%の検出は原理的に不可能**と判断し、
  現実的な工数で追加検証を打ち切った。
- **構造的に行ズレしている5セットを発見・全体空欄化**: `SVAL`/`SVAM`/`SVAW`/`SVHK`/`SVHM`
  （いずれも構築済み半デッキ系の型番のみのセットコード）は、日本語名の列に対して英語名の列全体が
  数行分ズレて入っていた（例: `SVHK`は+2行、`SVAM`/`SVAW`は+3行ズレ）。ズレた結果として
  **文法的に正しい英語のカード名が「別のカードの名前として」入ってしまう**ため、言語判定
  （インドネシア語検出）では原理的に発見できない。実例: `SVAM/017`の日本語名「きずぐすり」
  （Potion）に対し英語名は無関係な `Pokégear 3.0` になっていた。該当5セットの英語名列は
  全件空文字列にリセットした。同様の行ズレがSV4a内の一部区間（Trainerカード172-175番、
  クラベル/サカキのカリスマ/ナンジャモ/ネルケ周辺）にも局所的に見つかり、個別に空欄化した。
- **「クリーンなデータから対応表を作り空欄を埋める」復元方針は不採用（重要）**: 上記の行ズレ
  発見の過程で、**一見正しく見える cardData.json 内の(日本語名, 英語名)ペア自体に、既に
  誤ったペアが混在している**ことが判明した（`SVAM/017`の`きずぐすり→Pokégear 3.0`が好例。
  両方とも正規の英語カード名であるため、汚染検出をすり抜けて「クリーンなデータ」として
  対応表に採用されてしまう）。実際に対応表方式を試したところ2,018件を自動補完できたが、
  この誤ペアが伝播し、無関係なカードに別カードの正しい英語名が付与される事故を引き起こした
  ため、**適用前に発覚し全件revertした**。試作スクリプトは
  `scripts/deprecated/fill-blank-en-from-ja-map.mjs` に不採用の理由コメント付きで保存。
  **正しい英語名の復元には外部の権威あるソース（公式カードリスト等）との突き合わせが必須**で、
  cardData.json内のデータだけを信頼した復元は危険。
- **残存リスクについて**: 単語リスト＋構造的行ズレセットの全体空欄化により大部分の既知の
  汚染は除去したが、単語リスト方式の性質上、**検出漏れが皆無であるとは保証できない**。
  高額カードを出品する際は、生成された英語名が実際のカード名と一致しているか目視確認することを
  推奨する。

### 対応履歴（2026-08-03）: eBay出品効率化機能を追加

- **アイテムスペシフィック生成**（`buildItemSpecifics`）: フォーム入力から Card Name / Set /
  Card Number / Rarity（英語表記変換）/ Language / Manufacturer / Character（ex/V/VMAX等の
  接尾辞を除去） / Graded / Card Condition / Features（Old Back・1st Edition） /
  Year Manufactured（セットの`y`フィールド依存）を生成しコピーできるUIを追加。
  レアリティ英語表記（`RARITY_EN_LABELS`）はSV世代（Art Rare/Double Rare等）は公式表記と一致、
  それ以外は日本語圏コミュニティのベストエフォート訳である点に注意。
- **コンディション記述子ガイド**（`buildConditionGuide`）: 現在の状態設定がeBayの
  Ungraded/Gradedどちらに対応し、どのCard Condition/Grade等を選ぶべきかを案内する文言を表示。
- **SKU自動生成**（`buildSku`）: `{setCode}-{cardNo先頭}-{状態or鑑定}-{1ST}` 形式。
- **価格決定支援**: `computeRecommendedPrice`で目標利益率から売値を逆算し
  `roundUpToPsychologicalPrice`で$X.99に切り上げ（必ず入力値以上になる方向に丸める）。
  出品履歴から同一セット・レアリティの前回売値もヒント表示。
- **出品キュー + CSV一括アップロード（試験的機能）**: 出品キューをlocalStorage
  （`ptcg-ebay-tool:queue`）に保持し、`buildListingCsv`でFile Exchange相当の列構成のCSVを
  生成しダウンロード可能に。**`CSV_CONDITION_ID`（2750=Graded/4000=Ungraded）とカテゴリID
  （183454=CCG Individual Cards）はユーザー確認ベースの値であり、本番アップロード前に
  eBay側の最新テンプレートで要検証**。`PicURL`列は画像ホスティング環境が無いため空欄出力
  （手動で埋める前提）。ロット仕入れ値（1BOX÷枚数等）をキュー全件に一括適用する機能も追加。

### 未解決: 残り41枚

- **SVB/SVD/SVF/SLD/SLL/SN（24枚）**: 主にBOX付属の基本エネルギーカード。公式DBに個別カードとして掲載されていない（名前を直しても解決しない、画像ソースが存在しない）
- **SI/SV6/S11（11枚）**: 公式DB側の同名カード候補数と cardData 側の local 数が一致しないため、1:1フォールバックが使えないケース（例: 候補3件に対しlocalが2件）
- **PMCG5（2枚）/PMCG6（1枚）**: 「慈善」「レジスタンスジム」など、対応する公式サイト側のカードを特定できなかったトレーナーカード

### スキップセット（英語版 / 対象外）

SC*, SV*s, SVDs, SDL, SDM, SDP, CSMPiC

### 収録範囲の限界: DP・DPt・BW・ADV・LEGEND・XY・SM世代は非対応（2026-08-01調査）

`SERIE_ORDER`（`src/App.jsx`）には `XY, BW, DP, DPt, L, ADV, XYb` のキーが定義されているが、
実データ（`cardData.json`）には一切収録されていない。調査の結果、以下の理由で意図的に見送りとした:

- **TCGdex側**: JPシリーズ一覧は `PMCG/neo/VS/web/e/ADV/PCG/L/XY/XYb/SM/S/SV/M` の14種のみ存在するが、
  実際にカード個票データ（`/v2/ja/sets/{id}` の `cards` 配列）が入っているのは PMCG/neo/e/PCG/VS/web/M
  （旧世代）と S/SV（現行）のみ。ADV/L/XY/XYb/SM/DP/DPt/BW は**セットの入れ物だけあってカード個票が
  ほぼ空**（例外: CP1のみ34枚）。DP/DPt/BWはシリーズ自体がTCGdexに存在しない。
- **公式サイト側**: `pokemon-card.com` の `resultAPI.php` は `cardID/cardThumbFile/cardNameViewText` の
  4項目のみでカード番号を含まない。詳細ページ（`details.php`）も、この世代（例: DP1）は
  カード番号を表示するUI要素自体が存在しない（新しいテンプレートにのみある機能）。
  つまり**カード番号を検証する手段がどこにもない**。

このため、この世代のカードは「日本語名は取れるが、カード番号・レアリティ・英語名は検証不能」という
状態になり、出品事故防止の観点から追加を見送った。対応する場合は、掲載順を擬似カード番号として
使わざるを得ないことを理解した上で作業すること。

**【重要な訂正（2026-08-04）】この節の「公式サイト側」の結論はDP1に対する調査結果であり、
BW・XYには当てはまらないことが判明した。** `details.php`はBW・XY世代のカードには
カード番号を表示するUI要素が存在し（`No.001/060`相当の情報がHTML内に含まれる）、
番号の検証が可能。詳細は下記「調査（2026-08-04）: BW・XY世代のカード番号取得経路の検証」
を参照。DP・DPtについては`docs/task-bw-xy.md`が指摘する別の理由（コレクション番号が
種族単位の通し番号で1つの番号に複数カードが対応しうる）で依然として対象外。

### 対応履歴（2026-08-03）: インドネシア語混入の検出方式を単語リストから言語頻度方式に変更

外部レビューで、既存の `scripts/blank-contaminated-en-names.mjs`（単語リスト方式）では
78件の混入未検出が発見された。原因は方式そのものの限界: 単語リスト方式は「英語には現れない
インドネシア語」を列挙するため、`"Ursaluna Bulan Merah ex"`（正しい英語のポケモン名＋
インドネシア語の混成）のような**部分混入**は、混入した単語をリストに追加するまで検出できない
（列挙が原理的に終わらない）。

- **`scripts/detect-contaminated-en-names.py` を新規作成**: 各単語について
  `wordfreq` ライブラリで英語/インドネシア語それぞれの出現頻度（Zipf frequency）を計算し、
  「インドネシア語での頻度が高く、かつ英語との差が大きい」単語を機械的に混入とみなす方式に
  変更。単語を列挙する必要がないため、未知の混入語にも対応できる。
  `EXEMPT` セット（`lele`/`tapu`/`rotom`/`mega` 等、インドネシア語の一般語と衝突する
  英語ポケモン名由来のトークン）のみ手動管理。
- **dry-runで78件検出、`--apply`後は0件**: 検出0件になることを確認済み。
  セット別内訳（SV5s=11, SV8s=11, SV8a=10, SVDs=9, SV6s=6, SV9s=6, SV1V=4, SV3s=4,
  SV7s=4, SV1a=3, SV2a=3, SV4a=3, SV4s=2, SV1S=1, SV2D=1）は外部レビュー時の想定と完全一致。
- **旧スクリプトは `scripts/deprecated/` へ移動**し、非推奨の理由をコメントで明記。
- 復元は行っていない（`k[2]` を空文字列にリセットするのみ）。「絶対にやってはいけないこと」
  （対応表による補完）は今回も踏んでいない。

### 対応履歴（2026-08-03）: 英語名の表記ゆれ正規化

- **`scripts/normalize-en-names.mjs` を新規作成・実行**: 英語名末尾の角括弧注記
  （例: `Studio Berlatih[Stadium]`）を除去し、湾曲アポストロフィ・引用符
  （U+2018/2019/201C/201D）を直線形（U+0027/0022）に統一。10件が該当
  （角括弧3件: SV2a/197,199,207／アポストロフィ7件: `Farfetch’d` を含むセット）。
  実行後、`cardData.json` に `[` を含む英語名・U+2019を含む英語名は0件。
- **`src/App.jsx` の `normalizeBase` にも同じアポストロフィ置換を追加**（検索側の表記ゆれ吸収）。
  テスト用に `normalizeBase` を export した。

### 対応履歴（2026-08-03）: 行ズレ検査の回帰テスト化

過去に発見された「英語名の列が日本語名に対して数行ズレる」不整合（SVAL/SVAM/SVAW/SVHK/SVHM、
SV4aの一部）の再発を検知するため、`scripts/check-row-alignment.mjs` を新規作成した。

- **アルゴリズム**: (日本語名, 英語名) の組み合わせが2セット以上にまたがって出現するものだけを
  「正解ペア」として採用し、そのペアの日本語名を持つカードに限定して offset -4〜+4 での一致率を
  比較する。1セットにしか出現しない固有のカード名（他セットで裏取りできない）を照合対象から
  除外しないと、単に「珍しいカード名が多いセット」を誤検知することが実装時に判明した
  （M3/PMCG4/SV10/SV8a/SV9a等で疑似陽性が発生し、日本語名を持つカードのみに絞ることで解消）。
- **現状**: 実データ128セットに対して実行すると「疑いのあるセットはありませんでした」で
  exit code 0。
- **回帰テスト**: `scripts/check-row-alignment.test.mjs` を追加し `npx vitest run` に含めた。
  実データでの0件確認に加え、合成データで英語名列を意図的に2行ずらして検出できることを確認。

### 対応履歴（2026-08-03）: No Rarity（ノーレアリティ）印刷バリエーションの追加

日本版ベースセットには英語版の1st Editionに相当する「No Rarity」（カード右下のレアリティ
マークが無い最初期版）が存在するが、従来のツールにはこの概念が無く、旧裏カードの中でも
最も価格に影響する属性が表現できていなかった。

- **Web調査で対象範囲を確認**: PSA/Fanatics Collect等のグレーディング済みカード出品説明を
  複数確認したところ、"No Rarity Symbol" は一貫して「1996年の日本版拡張パック（Base Set）の
  初回印刷」を指すものとして扱われており、ポケモンジャングル/化石の秘密/ロケット団
  （PMCG2-4）やジム弾（PMCG5-6）に同種の情報は見つからなかった。このため
  `NO_RARITY_SET_RE = /^PMCG1$/i` として1st Edition（`PRINT_VARIANT_SET_RE`、PMCG1-6全体）
  より狭い範囲で実装した。
- **実装箇所（`src/App.jsx`）**: `PRINT_VARIANTS` に `No Rarity` を追加し、UIの選択肢は
  `NO_RARITY_SET_RE` に一致するセット（実質PMCG1のみ）でのみ表示。`resolvePrintVariant(f)`
  で「セットに対して有効な値か」を毎回検証し（履歴読み込み等でセットコードだけ後から
  変わった場合の防御）、タイトル・Item SpecificsのFeatures・SKU（`NR`サフィックス）に反映。
- 残課題: PMCG1以外にNo Rarity相当の版が存在するかは未確認のまま。追加情報が見つかれば
  `NO_RARITY_SET_RE` を広げること。

### 対応履歴（2026-08-03）: 現代カード（旧裏以外）のタイトル生成を優先度ベースに刷新

実売タイトル20件以上の調査（外部レビュー起点、CLAUDE.md末尾「補足」参照。因果は未検証）に
基づき、`buildSingleTitle` を書き換えた。

- **`SERIE_EN_NAMES`（`src/App.jsx`）を追加**: `SV`→"Scarlet & Violet"、`S`→"Sword & Shield"、
  `SM`→"Sun & Moon"、`M`→"Mega Evolution"。`M`（cardData.json上は2025-2026年発売のsr="M"の
  4セット）はThe Pokémon Company公式プレスリリース（press.pokemon.com「Mega Evolution
  Series」）とBulbapediaで確認済み。PCG/PMCG/e/neo/VS/web（旧裏世代）はセット単位の
  `enAlias`で別途対応する方針のためここには含めない。
- **`buildSingleTitle` はディスパッチャに変更**: `OLD_BACK_SET_RE`に一致するセットは
  （タスク5で置き換え予定の）`buildLegacyOldBackTitle`（旧実装をそのまま温存）に委譲し、
  それ以外は新設の `buildModernTitle(f, { maxLength = 80 })` を使う。
- **`buildModernTitle`**: 要素を優先度順（鑑定情報→カード名→レアリティ略号→カード番号→
  Japanese→セット型番→"Pokemon Card"→状態→セット英語名→発売年→Holo→レアリティフルスペル）
  で組み立て、80文字を超える場合は優先度の低いもの（レアリティフルスペル→Holo→発売年→
  セット英語名の順）から1つずつ落とす。優先度1〜8の必須要素は文字数超過時も削らない。
  - `"Pokemon Card"`の重複対策として、セット英語名に含まれる"Pokemon"という単語は
    シリーズ英語名と組み合わせる前に取り除く（例: setNameEn="Pokemon 151" + serieEn=
    "Scarlet & Violet" → "Scarlet & Violet 151"）。
  - 発売年・シリーズ名は `f.setNameEn` ではなく `CARD_DATA` から都度引き直す
    （フォームの値が古いままでも正しい年を反映するため）。
  - `getDroppedModernTitleParts(f, opts)` を追加し、80文字調整で省略した要素を
    日本語ラベルの配列で返す。UIで「80文字に収めるため、〜を省略しました」と表示する
    （タスク4-3）。
- 既存の「英語名引き継ぎ事故防止」等の安全対策（`applyCandidateToForm`等）には影響しない。

### 対応履歴（2026-08-03）: 旧裏（Old Back）専用タイトル・アイテムスペシフィックの追加

タスク5の残り（5-2〜5-6）に対応。旧裏12セット（`OLD_BACK_SET_RE`: PMCG1-6 / neo1-4 /
VS1 / web1）は現代カードと検索のされ方が根本的に異なるため、専用のテンプレートを実装した。

- **セット英語通称（enAlias、5-2）**: `cardData.json`のうちen（公式英語名）が空の6セットに
  `enAlias`フィールドを追加（`scripts/add-old-back-en-alias.mjs`）。
  PMCG1→"Base Set"、PMCG5→"Leaders' Stadium"、PMCG6→"Challenge from the Darkness"、
  neo2→"Neo Discovery"、VS1→"Pokemon VS"、web1→"Pokemon Web"。いずれもWeb調査で
  複数の収集コミュニティサイト（tcgcollector.com/pokemonplug.com/Bulbapedia等）が
  一貫して使用していることを確認済み（スクリプト内コメントに出典を記載）。
  PMCG5/6は英語版Gym Heroes/Gym Challengeがこの2弾の内容を再編成したものでカードプールが
  1:1対応しないため、その訳語は採用しなかった。VS1/web1は英語版が発売されていないため
  「英語版セット名」ではなく収集コミュニティの通称。
  `applyCandidateToForm`を`r.set.en || r.set.enAlias || ""`に変更し、公式英語名が
  無い場合のみenAliasにフォールバックする。
- **カード番号の表示形式（5-3）**: 旧裏の実売タイトルでは`No.006`形式が主流。ただし
  `cardNoOf`が返す`f.cardNo`自体（"006/102"形式）を変更すると、SKU生成・
  Item SpecificsのCard Number・`entryImage`（履歴/キューのサムネイル画像検索。
  `cardNo.split("/")[0]`を`parseInt`する実装のため、"No.006"形式だと`NaN`になり
  画像が表示されなくなる）に影響するため、**内部の`f.cardNo`は変更せず**、
  `buildVintageTitle`内の`vintageCardNoToken()`でタイトル表示時のみ`No.006`に変換する
  実装にとどめた（当初案の「cardNoOf自体を切り替える」からの意図的な変更点）。
- **`buildVintageTitle`（5-4）**: 優先度順（鑑定情報→カード名→No.番号→セット通称→
  印刷バリエーション→Japanese→発売年→Old Back→"Pokemon Card"→レアリティ語→状態）で
  組み立て、80文字超過時はレアリティ語→状態の順に落とす（この2つのみ削除可）。
  レアリティのフルスペル（Special Art Rare等）は現代カードのみの概念のため使わない。
  カードのレアリティ表記（Rare/Uncommon/Common）は`cardData.json`の簡易レアリティ
  （C/U/R）から機械的に生成するのみで、**「Holo」の有無は推測で付け足していない**
  （当時は同じ"R"マークでホロ・非ホロが混在しており、cardData.jsonにその区別を
  記録するフィールドが無いため）。`buildSingleTitle`は`OLD_BACK_SET_RE`一致セットを
  `buildVintageTitle`に、それ以外を`buildModernTitle`に振り分けるディスパッチャに整理。
- **発売年の補完（5-5）**: 旧裏12セットは調査の結果、**全セットで既に`y`が入っていた**
  （PMCG1=1996〜web1=2001）。追加のデータ補完は不要だった。
- **Vintage判定（5-6）**: `buildItemSpecifics`に`Vintage: Yes`を追加。eBayの
  「Vintage」判定に使われる年数のしきい値は公式に確認できなかったため、年号による
  判定は行わず**`OLD_BACK_SET_RE`に一致するセットであることのみを根拠にYesとする**
  （閾値を推測で決めることは「絶対にやってはいけないこと」に該当するため）。
  CSV出力（`buildListingCsvRow`）へのVintage列追加は本タスクのスコープ外として見送った。
- ブラウザで実際の検索→旧裏チェック→No Rarity選択→タイトル/アイテムスペシフィック反映
  （`Charizard No.021 Base Set No Rarity Japanese 1996 Old Back Pokemon Card Rare NM`、
  Item Specifics上の`Set: Base Set` `Vintage: Yes`）を確認済み。

### 調査（2026-08-04）: BW・XY世代のカード番号取得経路の検証（ステップ1、`docs/task-bw-xy.md`）

「収録範囲の限界」セクションでDP1に対して行った調査（番号取得手段なしと判断）が、
BW・XY世代でも同じ結論になるかを再検証した。**結論: 経路Aが成功し、番号取得可能と判明**。
DP1の調査結果をBW・XY全般に外挿していたのは誤りだった。

- **ステップ0（リポジトリのprivate化）**: 着手前に確認したところ、
  `yoichi-project01/ptcg-ebay-tool`は**既にprivateだった**（認証済みAPIで
  `"private": true`を確認）。依頼書内の「現在public」という記述は古い状態を
  指しており、この調査の時点では前提が既に満たされていた。
- **経路A（`details.php`にカード番号があるか）: 成功**。XY1（コレクションX/Y、
  `official-card-cache.json`上の`setCode`は`XY1-Bx`/`XY1-By`）のカードで検証。
  `details.php/card/{cardId}`のHTMLに`<img class="img-regulation" alt="XY1-Bx" />
  &nbsp;001&nbsp;/&nbsp;060&nbsp;`という形で**カード番号・総数が両方とも表示されており**、
  既存の`fetchCardLocalNumber()`の正規表現（`img-regulation"[^>]*\/>\s*&nbsp;(\d+)&nbsp;`）
  がそのまま流用できることを確認した。DP1で「新しいテンプレートにのみある機能」として
  存在しなかった要素が、BW・XY世代（DPより新しい）では存在する。
  - XY1-Bx/XY1-By全126枚（各63枚 = 通常60枚+シークレット3枚）について、
    `cardId`を1件ずつ`details.php`で引いて「番号・総数・日本語名」の対応表を作成し、
    エラー0件で完全に取得できた（`CONCURRENCY=3`/`DELAY_MS=400`/
    `AbortSignal.timeout(15000)`遵守、127回のリクエストで失敗0件）。
  - 番号は各コレクションで`001`〜`060`が欠番・重複なく揃い、シークレットカード3枚が
    `061`〜`063`（総数`060`のまま）に収まっていた。総数<最大番号のパターンは
    依頼書の想定通り。
  - 12枚（両コレクションの先頭・中間・末尾・シークレット×各6枚）を画像でダウンロードし
    目視照合。**12/12件で印刷されている番号・カード名が`details.php`の取得結果と完全一致**
    （例: `XY1-Bx`の`001/060 RR`＝フシギバナEX、`063/060 SR`＝ゼルネアスEX、
    `XY1-By`の`060/060 U`＝ダブル無色エネルギー等）。
  - 副次的な発見: `official-card-cache.json`（2026-07-04生成、既存の全件スキャンキャッシュ）
    には**既にXY/BW/DP era の`setCode`が322セット中の一部として含まれていた**
    （`XY1-Bx`,`XY1-By`,`XY2`〜`XY11`,`XYA`〜`XYH`,`BW`,`DP`,`DP1`〜`DP5`,
    `DPt`系等）。つまり公式サイトの検索DB自体にはこれらの世代のカードが既に
    登録されており、「収録範囲の限界」の消極的結論は主に「番号の検証手段が無い」ことが
    理由であり、その前提が経路Aの発見で崩れたことになる。
  - もう一つの副次的発見: 資材キャッシュの`cardNameViewText`（`jaNameViewText`）は
    メガシンカポケモンで`<span class="pcg pcg-megamark"></span>フシギバナEX`のような
    アイコン混じりの表記になっており、`details.php`の`<title>`タグでは
    `メガフシギバナEX`とテキストで書かれている（126枚中2枚で不一致を検出、
    いずれもこのパターン）。日本語名は`details.php`側を正とするほうが安全。
  - カード左下に共通して`1 EDITION`という縦書きマークが確認できた（フシギバナEX含む
    通常カード・エネルギーカードすべてに付いている）。1996年Base Setの1st Editionとは
    別物と思われる。ステップ2（データモデルの確認）で扱いを判断すること。
- **経路B（掲載順＝番号順の仮説）: 検証不要と判断（経路Aが成功したため）**。
  ただし経路Aの調査中に、`official-card-cache.json`内の配列順序が番号順になっていない
  ことを偶然確認した（`resultAPI.php`は`sortBy=new`で取得しているため、後から追加された
  カード＝シークレットカードほど配列の前方に来る）。**この仮説を採用していたら誤った
  対応付けになっていた**ことの実例的な裏付けが取れた。
- **経路C（TCGdexの再確認）: 引き続き失敗**。`https://api.tcgdex.net/v2/ja/series/xy`は
  `XY1a`（コレクションX, cardCount.official=60）を含む一覧を返すようになっており、
  「収録範囲の限界」調査時点より**セットの入れ物（メタデータ）は増えている**。しかし
  `https://api.tcgdex.net/v2/ja/sets/xy1a`の`cards`配列は依然として空（0件）で、
  カード個票データは無いまま。ステップ1の判断には影響しない（経路Aで足りるため）。
- **目視照合の手間**: 経路Aにより番号がAPI側で検証済みのため、経路Bのような
  「1枚ずつ画像を見て番号を読み取る」作業は不要。受け入れ条件で求められる
  「10枚以上の目視照合」は数分で完了した（ダウンロード→画像確認のみ）。
  1セットあたりの追加コストは既存スクリプトのパターン踏襲で吸収できる見込み。
- **調査で使ったスクリプト・データはリポジトリに含めていない**（`scratchpad`配下の
  使い捨てスクリプトのみで作業し、`scripts/`や`cardData.json`は一切変更していない。
  `git status`で無変更を確認済み）。

**判断: ステップ2（データモデルの確認）に進む価値がある。** 番号取得経路そのものは
解決済み。次に着手する場合は、上記の`1 EDITION`マークの扱い（依頼書のステップ2-3
「エキスパンションマーク」の論点とは別に、この縦書きマークの意味も要確認）と、
`XY1-Bx`/`XY1-By`のような「1つの商品(XY1)が複数setCodeに分かれる」ケースの
データモデル上の扱いを先に詰めること。

### 調査（2026-08-04）: BW・XY世代のデータモデル確認（ステップ2、`docs/task-bw-xy.md`）

ステップ1の続き。BW1・XY5周辺・複数のXY/BW世代セットを追加調査し、既存のデータモデル
（`cardNoOf`/`buildSku`/`buildItemSpecifics`/`buildSingleTitle`）がそのまま使えるかを判断した。
**結論: 新しい分岐（旧裏パターンの複製）は不要。既存の「現代カード」経路にそのまま乗る。**
ただし`SERIE_EN_NAMES`へのキー追加とレアリティ英語表記の見直しは必要。

- **1. 番号形式（BW1で確認）**: `BW1-Bb`/`BW1-Bw`（`official-card-cache.json`上のsetCode）
  で6件サンプリングし、`details.php`で`001/053`〜`053/053`の「NNN/NNN」形式を確認。
  XY1と同一形式で、欠番・重複なし（BW1にはシークレットカードが無く、総数=最大番号で一致）。
  総数<最大番号（シークレットカードの存在）は依頼書の想定通りで、既存の`cardNoOf`
  （`setObj.of`＝総数、localが最大番号を超えてもそのまま`pad3`で表示するだけ）が
  **無変更でそのまま使える**ことを確認した。
- **2. 「1 EDITION」マークの扱い（最重要）— printVariantとして持たせないことを確認**。
  21枚を画像で目視確認（ステップ1の12枚 + 今回9枚、BW1×4／XY2×1／XY6-B×1／XY11×1／
  XYA×2／XYB×2／XYC×1、レアリティも C/U/R/RR/SR と幅広くカバー）した結果、
  **「拡張パック本弾（番号付き商品: XY1, XY2, BW1, XY6-B, XY11等）に属するカードは
  レアリティ・カード種別を問わず全件に付いている」「XYA〜XYHのような文字のみの
  エキスパンションマーク単独セットには一件も付いていない」**という明確な二値パターンを
  確認した（2010年のBW1〜2016年のXY11まで一貫）。つまりこのマークは
  「商品の初回印刷ロットである」ことを示す一律のスタンプであり、**カードの希少性や
  版の違いを表す情報ではない**。ユーザー指示通り、`printVariant`（旧裏の1st Editionと
  同じ枠）としては持たせず、タイトル・アイテムスペシフィックにも含めない方針とする。
  なお、この二値パターンはHTMLレベルの`ic_rare_*.gif`（レアリティアイコン）の有無とも
  完全に一致していた（後述5参照）。マークの正体（何を表すか）は未確定のままだが、
  「全カード一律」ではなく「商品カテゴリに一律」という分布が分かったことで、
  価格情報として扱わない判断には十分な根拠が得られた。
  **注意（未確認事項）**: 公式サイトの画像は1カードにつき1枚しか掲載されていないため、
  今回確認できたのは「同一商品内での分布」のみ。「後年の無印（1 EDITIONマーク無し）
  再版が存在しないこと」自体は確認できていない。
- **3. エキスパンションマークとsetCode — マーク準拠が妥当、かつ実質的に追加設計不要**。
  `official-card-cache.json`を調べたところ、公式サイトの画像格納フォルダ（＝setCode）は
  **既にマーク準拠で分かれていた**。依頼書が例示した「XY5『ライジングフィスト』収録の
  サナ・ティエルノ」を検索したところ、この2枚は`XY5-Bg`/`XY5-Bt`（依頼書が言う
  「XY5」に対応する番号付き商品のsetCode）には存在せず、独立した`XYA`
  （21枚、`details.php`で確認: サナ=017/021, ティエルノ=018/021）に格納されていた。
  つまり**「setCodeをマーク準拠にする」という依頼書の案は、公式サイト自身のデータ構造と
  一致しており、追加の設計判断は不要**（「収録商品」を別フィールドで持つ仕組みを
  新設する必要もない。既存のcardData.jsonにもどのセットにも「収録商品」フィールドは
  無く、この点は他の全セットと同じ扱いで済む）。
  - 【訂正】上記で「依頼書の記述と食い違う」としたのはこちらの誤認。ユーザー確認により、
    「ライジングフィスト＝XY3」というTCGdex側の表記が正しく、依頼書の「XY5」表記が
    誤りだった。TCGdexの内部ID自体は信頼してよい。setCodeをマーク準拠にするという
    結論には変わりがない。
  - 同様の構造（バラのセットとは別に独立した文字コードのグループが存在する）は
    `XYA`〜`XYH`（8種）・`BWA`ではなく`BW`のみ確認、他にも`XY6`（11枚、`XY6-B`の78枚とは別）
    や`XY7`（11枚、`XY7-B`の84枚とは別）のように、番号付きセットのコード自体が
    メガシンカEX等の小規模な別グループを内包しているケースもあった。ステップ4の
    横展開時、setCode一覧は`official-card-cache.json`のキーから機械的に列挙すればよく、
    推測で命名する必要はない。
- **4. メガシンカの名前表記 — details.php + decodeHtmlEntities()の徹底が必要**。
  XY1の126枚中2件で`official-card-cache.json`（`resultAPI.php`由来）の`cardNameViewText`が
  `<span class="pcg pcg-megamark"></span>フシギバナEX`のようなアイコンHTML混じりになって
  おり、`details.php`の`<title>`タグでは`メガフシギバナEX`とテキスト化されていることを
  確認済み（ステップ1参照）。今回追加で確認: XY1の126枚の`<title>`には`&`等のエンティティを
  含む例が無かったが、**過去のS-P TAG TEAMカード（グズマ&ハラ等）の実例がある以上、
  「今回のサンプルに無かった」ことは「今後も出ない」ことを意味しない**。ステップ3の
  `scrape-bw-xy.mjs`では、`title`から名前を取り出す箇所に無条件で既存の
  `decodeHtmlEntities()`を通すこと（サンプルに実例が無くてもコストゼロの防御なので、
  条件分岐せず常に適用する）。
- **5. レアリティ — 既存RARITIESで足りる（BREAKの根拠は見つからず）**。
  `details.php`はカード番号の直後に`<img src="/assets/images/card/rarity/ic_rare_{code}.gif">`
  という**機械可読なレアリティアイコン**を埋め込んでいることを新たに発見した
  （これも`resultAPI.php`には無い情報で、`details.php`側だけの利点）。全XY/BW系
  セット（番号付き・文字マーク・裸のBW/XY表記を含む）から180枚を無作為抽出して
  コード分布を調べたところ、実際に出現したコードは`c_c`(C)/`u_c`(U)/`r_c`(R)/`rr`(RR)/
  `sr_c`(SR)/`ur_c`(UR)の6種のみで、**すべて既存の`RARITIES`配列（`SAR, SR, AR, UR,
  RR, RRR, CHR, CSR, HR, SSR, S, ACE, BWR, R, U, C, PROMO`）に既に含まれていた**。
  依頼書が例示した「BREAK」については、カード名に`BREAK`という文字列を含むカードが
  `official-card-cache.json`全体（BW/XY以外の世代も含む）を検索しても1件も
  見つからず、レアリティアイコンの調査でも新規コードは出現しなかった。BREAK進化は
  カードの進化形態（メカニクス）であってレアリティ区分ではない可能性が高く、
  **現時点でRARITIES配列に追加が必要という証拠は見つかっていない**。ただし
  BREAKカード自体を直接特定できていないため「存在しないと断定」はできない。
  ステップ3でXY6〜XY9系（BREAK世代とされる）を実際に取り込む際に、
  想定外のレアリティコードが出た場合はその時点で追加すること。
  - 【訂正・既知バグとして記録】当初「BW/XY世代の`SR`は"Secret Rare"を指すのでは」と
    推測したが、ユーザー確認により誤り。日本語版の`SR`は全世代を通じて「スーパーレア」
    であり、世代差は無い。**つまり現在の`RARITY_EN_LABELS`の`SR: "Special Rare"`は
    世代に関係なく誤訳で、正しくは`"Super Rare"`。この誤りはBW/XYだけでなく既存の
    SV世代の出品にも影響している既存バグ**。今回のタスクでは修正せず、課題として
    ここに記録するのみとする（`RARITY_EN_LABELS`全体の見直しは別タスクとする）。
  - 180枚のうち56枚（レターマーク単独セット40枚＋裸の`BW`/`XY`表記8枚）はレアリティ
    アイコン自体が存在しなかった。これは1 EDITIONマークの有無の分布（2番目）と
    **完全に一致するパターン**であり、「番号付き商品（拡張パック本弾）」と
    「それ以外（文字マーク単独／裸のBW・XY）」を区別する追加の手がかりになる。
    また裸の`BW`/`XY`というsetCode自体が何を表すか（重複登録の索引的カテゴリの
    可能性）は未解明で、対象を「拡張パック本弾のみ」に絞る依頼書の方針と合わせて
    今回は深追いしていない。
- **6. 発売年（yフィールド）— XYは取得可能、BWは取得経路なし**。
  TCGdex（`/v2/ja/series/xy`→`sets`）はXY系の番号付きセット（`XY1a`〜`XY7`、
  `CP1`/`CP2`）について`cards`配列こそ空だが**`releaseDate`はセット単位で
  正しく入っていた**（例: `XY1a`＝2013-12-13）。既存の`scripts/fetch-set-years.mjs`
  と同じ経路（`/v2/ja/sets/{id}`の`releaseDate`）がそのまま使える。
  一方BWについては、TCGdexのシリーズ一覧（`/v2/ja/series`）に`BW`という
  シリーズ自体が存在しない（「収録範囲の限界」の既存記述通り、今回の再調査でも
  変化なし）。**BW世代の発売年はTCGdexからは取得できず、手動登録が必要**。
  なお`XYA`〜`XYH`のような文字マーク単独セットも、TCGdexのXYシリーズ一覧には
  含まれていなかった（番号付きセットのみ掲載）ため、対応する場合は同様に手動になる。

**既存関数がそのまま使えるかの判断**:

| 関数 | 判断 | 理由 |
|---|---|---|
| `cardNoOf` | **無変更で使用可** | `NNN/NNN`形式・シークレットで総数超過、既存ロジックと同じ挙動 |
| `buildSku` | **無変更で使用可** | 1 EDITIONをprintVariantにしないため、分岐要素が増えない |
| `buildItemSpecifics` | **無変更で使用可（ただし`y`の投入が前提）** | `OLD_BACK_SET_RE`に該当しないため`Vintage`判定は自動的にNo扱いになり、これは正しい（BW/XYは新裏カードで旧裏ではない） |
| `buildSingleTitle` | **無変更で使用可（buildModernTitle経路）** | `OLD_BACK_SET_RE`はPMCG/neo/e/VS/web専用で、BW/XYは元々マッチしないため自動的に`buildModernTitle`に振り分けられる。旧裏と同じ分岐パターンの複製は不要 |

**唯一必要な追加（分岐ではなくデータ追加）**: `SERIE_EN_NAMES`（`src/App.jsx`）に
`BW`→（英語版シリーズ名を要調査。west版"Black & White"と1:1対応するか未確認）、
`XY`→"XY"のキーを追加すること。現状は`SV/S/SM/M`の4キーのみで、BW/XYが無いため
`buildModernTitle`のセット英語名にシリーズ名が前置されない（動作はするが検索露出が
下がる）。西欧版とのシリーズ名対応関係の確認はステップ3着手時に行うこと。

**ステップ2の結論**: 依頼書が想定していた「旧裏と同じパターンの新規分岐」は不要と判明した。
最大の懸念事項だった「1 EDITIONマークの誤解釈」はデータで否定でき、setCode設計も
公式サイトの実データ構造にすでに沿っている。残る作業はほぼ「データ投入」（`y`フィールド、
`SERIE_EN_NAMES`のキー追加）で、ロジック変更はほとんど発生しない見込み。
ステップ3（XY1の取り込み）に進む価値があると判断する。

### ステップ2への訂正（ユーザー確認、2026-08-04）

- **「XY5 ライジングフィスト」は依頼書側の誤り**。TCGdexの表記（`XY3`＝ライジングフィスト）が
  正しいことをユーザーが確認済み。setCodeをマーク準拠にする結論自体は変わらない
  （上記「3. エキスパンションマークとsetCode」の該当箇所を訂正済み）。
- **「BW/XY世代のSRはSecret Rareを指す」という推測は不採用**。日本語版のSRは
  全世代を通じて「スーパーレア」であり世代差は無い。つまり既存の`RARITY_EN_LABELS`の
  `SR: "Special Rare"`は**世代に関係なく誤訳で、正しくは`"Super Rare"`**。BW/XYだけでなく
  既存のSV世代の出品にも影響している既存バグだが、今回のタスクでは修正しない
  （`RARITY_EN_LABELS`全体の見直しは別タスク）。**課題として記録するのみ**
  （上記「5. レアリティ」の該当箇所を訂正済み）。
- **「1 EDITION」マークの分布について未確認事項がある**: 公式サイトの画像は1カードにつき
  1枚しか掲載されていないため、確認できたのは「同一商品内での分布」のみ。
  「後年の無印（マーク無し）再版が存在しないこと」自体は確認できていない
  （上記「2. 1 EDITIONマークの扱い」に注記を追加済み）。

### 対応（2026-08-04）: ステップ3 — XY1（コレクションX/Y）の取り込み完了

`scripts/scrape-bw-xy.mjs`を新規作成し、XY1-Bx（コレクションX）/XY1-By（コレクションY）
計126枚を`cardData.json`に追加した。ステップ4（横展開）には着手していない。

- **設計: ホワイトリスト方式**。ユーザー指示により、`official-card-cache.json`の
  キーを機械的に列挙するのではなく、`TARGET_SETS`定数に明記したsetCodeのみを処理する
  設計にした。裸の`BW`/`XY`という正体不明のsetCode（ステップ2調査でレアリティアイコン・
  1 EDITIONマークとも一貫して存在しないことを確認済み。番号付き商品とは異なる素性不明の
  カテゴリと思われる）が誤って混入することを防ぐため。ステップ4を行う場合は
  `TARGET_SETS`に1セットずつ追記する運用にする。
- **番号取得**: `official-card-cache.json`（`resultAPI.php`由来、既存の全件スキャン
  キャッシュを再利用。新規の全件再スキャンは行っていない）でカードID一覧を取得し、
  1枚ずつ`details.php`で番号・総数・レアリティ・日本語名を検証した
  （経路A、CLAUDE.md「調査（2026-08-04）」参照）。`CONCURRENCY=3`/`DELAY_MS=400`/
  `AbortSignal.timeout(15000)`を遵守。126枚全件で番号取得に成功（失敗0件）。
  取得後に「番号の重複」「欠番（1〜総数の範囲内）」「総数の不一致」「未知のレアリティ
  コード」を機械的に検証し、いずれかがあれば例外を投げて`cardData.json`への書き込みを
  中断する設計にした（絶対にやってはいけないこと: 未検証データの投入、を仕組みで防ぐ）。
- **レアリティ**: `details.php`のレアリティアイコン画像ファイル名
  （`ic_rare_{code}.gif`）から機械的に取得。XY1-Bx/By各セットの内訳は
  `C:30 / U:18 / R:8 / RR:4 / SR:3`（各63枚）で一致しており、想定外のコードは無かった
  （ステップ2の事前調査通り、`UR`はXY1には出現しなかった）。
- **日本語名**: `details.php`の`<title>`タグから取得し、`decodeHtmlEntities()`を
  無条件で適用（サンプルに実例が無くてもコストゼロの防御として常時適用、ユーザー指示通り）。
- **英語名**: 取得していない（`en: ""`）。ユーザー指示通り、信頼できるソースが無いため
  空欄のままとした。
- **発売年（`y`）**: TCGdex（`https://api.tcgdex.net/v2/ja/sets/xy1a`,`xy1b`の
  `releaseDate`＝2013-12-13）から`y: 2013`を確認済みの値としてスクリプト内に直書きした
  （2セットのみのため専用の取得ロジックは作らず、値をコメントに出典付きで記載）。
- **画像**: `filename-utils.mjs`の`isUsableImage`/`writeFileAtomic`/`MIN_IMAGE_BYTES`を
  使用し、126枚全件を`public/cards/XY/XY1-Bx/`・`public/cards/XY/XY1-By/`にダウンロード
  （失敗0件）。`node scripts/build-image-index.mjs`で`imageIndex.json`を再生成し、
  126枚全件がインデックスされたことを確認した。
- **受け入れ条件の確認結果**:
  - `node scripts/check-row-alignment.mjs` → 「疑いのあるセットはありませんでした」（exit 0）
  - `python3 scripts/detect-contaminated-en-names.py` → 0件（英語名が空なので当然だが確認済み）
  - XY1-Bx/By合わせて10枚を無作為抽出し、ダウンロード済み画像を目視確認。
    全10枚で番号・カード名・レアリティが`cardData.json`の内容と一致することを確認
    （フシギバナEXの通常版001/060 RRと秘密版061/060 SR、異なるイラストの2種を含む）
  - `npx vitest run` → 104件全通過（変更なし）
  - `git diff src/cardData.json` → 776行すべて追加（挿入）のみ、既存セットの削除・変更は0行
  - `npm run build` → 成功
  - ブラウザで実際に検索→カード選択→タイトル/説明文/アイテムスペシフィック生成を確認
    （`R 021/060 Japanese XY1-Bx Pokemon Card NM 2013 Rare`、Year Manufactured: 2013、
    Rarity: Rare）。コンソールエラー無し。既存の`buildModernTitle`/`cardNoOf`/`buildSku`/
    `buildItemSpecifics`は無変更のまま正しく動作し、ステップ2の判断
    （新規分岐は不要）が実地でも裏付けられた。
- **未対応のまま残した点**（ステップ4以降の課題、下記の追加対応で一部解消）:
  - `RARITY_EN_LABELS`の`SR`表記の誤り（上記訂正セクション参照）は未修正
  - 裸の`BW`/`XY`というsetCodeの正体は未解明のまま

### 対応（2026-08-05）: ステップ3の追加確認3点（ステップ4着手前の設計確定）

ステップ3完了報告に対しユーザーから3点の確認指示があり、対応した。

**1. カードに印刷されている型番の確認（最重要・重要なバグ修正）**

動作確認で見えたタイトルが内部setCode（`XY1-Bx`）をそのまま出力しており、
「海外バイヤーはこの文字列で検索しない」との指摘を受けて調査した。

- ダウンロード済み画像をPythonで右下の型番バッジ部分を切り出し・拡大して目視確認
  （XY1-Bx側4枚、XY1-By側2枚、通常レアリティ〜シークレットレアまで計6枚）。
  結果: **印刷されている型番はコレクションX/Yとも同一の"XY1"**（バッジの背景色のみ
  青(X)/赤(Y)で異なる。テキスト自体は完全に同じ）。`XY1-Bx`/`XY1-By`は公式サイトが
  X版/Y版を区別するために振った内部フォルダ名であり、カード面には現れないことが確定した。
- **対応**: `cardData.json`の`XY1-Bx`/`XY1-By`双方に`codeAlias: "XY1"`を追加した
  （旧裏セットの`enAlias`と同じ構造の横展開）。`setCode`自体は`XY1-Bx`/`XY1-By`のまま
  変更していない（X/Y版を一意に区別する内部識別子として、SKU生成・画像パス検索
  （`imageIndex.json`のキー）・履歴の同一カード判定など内部処理に使い続けるため）。
- `src/App.jsx`に`displaySetCode(setCode)`ヘルパーを新設し、`CARD_DATA`から
  `codeAlias`があればそれを、無ければ`setCode`をそのまま返すようにした
  （`enAlias`と同じ`||`フォールバックパターン）。**買い手向けの出力4箇所**
  （`assembleModernTitle`のタイトル・`buildPackTitle`・`buildEbaySearchQuery`
  （シングル/パック双方）・`buildSingleDesc`/`buildPackDesc`の`Set: ... (型番)`表記）に適用し、
  内部setCodeがバイヤー向けテキストに漏れないようにした。**SKU生成
  （`buildSku`）・画像パス検索（`entryImage`）・履歴の同一カード判定・フォーム入力欄・
  履歴一覧のUI表示は意図的に変更していない**（内部識別子としてsetCodeをそのまま使うことで
  X/Y版の一意性を保つ必要があるため）。
- `codeAlias`未設定の既存126セットは`displaySetCode`が単に`setCode`を返すため、
  **動作は一切変わらない**（回帰テストで確認済み）。
- ブラウザで再確認: タイトルが`... XY1 Pokemon Card NM 2013 Rare`（`XY1-Bx`は非表示）、
  説明文が`- Set: [Set name] (XY1)`、SKUは引き続き`XY1-BX-021-NM`のまま、と確認した。

**2. 英語名なし警告の動作確認**

XY1のカード（`en`空欄）を選択した状態で、タイトルカードに
`⚠ 英語名・セット英語名がデータに未登録です。前に選んだカードの英語名が残っていないか
確認し、手入力してからコピーしてください。`という警告が表示されることをブラウザで確認済み
（既存の`missingEn`ロジックはBW/XY追加に伴う変更をしていないため元々動作する設計だったが、
実地で再確認した）。

**3. `SERIE_EN_NAMES`への`XY`/`BW`追加**

TCGdexの英語版シリーズ一覧（`https://api.tcgdex.net/v2/en/series`）で
`xy`→`"XY"`、`bw`→`"Black & White"`という公式表記を確認できたため、両方とも追加した
（`M`シリーズの英語名確認と同様、TCGdex側の権威あるソースで裏取りできたため採用）。
現状はXY1の`en`/`enAlias`がいずれも空のため、`buildModernTitle`のセット英語名トークンには
まだ反映されない（将来英語名を埋めたときに効いてくる）。

**受け入れ確認**: `npx vitest run`は109件全通過（104件 + 今回追加5件）、
`check-row-alignment.mjs`・`detect-contaminated-en-names.py`・`npm run build`も
問題なし。`git diff src/cardData.json`は追加のみ（`codeAlias`2行の追加を含め778行、
削除0行）で既存データは無変更。`scripts/scrape-bw-xy.mjs`の`TARGET_SETS`にも
`codeAlias: "XY1"`を追記済みで、ステップ4で同スクリプトを再実行しても同じ設計が
引き継がれる。

**ステップ4着手前に確定した設計**: 新規セットを追加する際は、画像を目視確認して
「セットコード（内部識別子）」と「カードに印刷されている型番」が食い違わないか
毎回確認し、食い違う場合は`codeAlias`を追加すること。

### 対応（2026-08-05）: ステップ4横展開 — XY世代 拡張パック本弾13セットを追加

`docs/task-bw-xy.md`のステップ4（横展開）に着手。XY世代の拡張パック本弾を全て取り込んだ
（BW世代は未着手、本節末尾の理由により着手前に報告・停止）。

**取り込んだセット一覧**（setCode / codeAlias / 枚数 / 発売年）:

| setCode | codeAlias | 枚数 | 発売年 | ja |
|---|---|---|---|---|
| XY1-Bx | XY1 | 63 | 2013 | コレクションX（ステップ3で対応済み） |
| XY1-By | XY1 | 63 | 2013 | コレクションY（ステップ3で対応済み） |
| XY2 | XY2 | 87 | 2014 | ワイルドブレイズ |
| XY3 | XY3 | 103 | 2014 | ライジングフィスト |
| XY4 | XY4 | 95 | 2014 | ファントムゲート |
| XY5-Bg | XY5 | 78 | 2014 | ガイアボルケーノ |
| XY5-Bt | XY5 | 78 | 2014 | タイダルストーム |
| XY6-B | XY6 | 89 | 2015 | エメラルドブレイク |
| XY7-B | XY7 | 95 | 2015 | バンデットリング |
| XY8-Bb | XY8 | 64 | 2015 | 青い衝撃 |
| XY8-Br | XY8 | 64 | 2015 | 赤い閃光 |
| XY9-B | XY9 | 88 | 2015 | 破天の怒り |
| XY10-B | XY10 | 87 | 2016 | めざめる超王 |
| XY11-Bb | XY11 | 58 | 2016 | 爆熱の闘士 |
| XY11-Br | XY11 | 58 | 2016 | 冷酷の反逆者 |

合計 15セット・1170枚（XY1除く13セット・1044枚が今回の新規追加分）。

**codeAliasがsetCodeと一致しなかったセット**: 全13セットすべて。印刷されている型番は
一貫して「setCodeから`-B`以降のサフィックスを除いたもの」（`XY5-Bg`→`XY5`、
`XY8-Bb`→`XY8`等）だった。バッジの色のみコレクション間で異なる（例: XY8-Bbは青、
XY8-Brは赤）。全13セットとも画像を切り出し・拡大して目視確認済み（`XY2`〜`XY11`は
それぞれ1枚ずつバッジを確認、`XY1`はステップ3で確認済み）。推測ではなく毎回確認する
方針を徹底したが、結果的に「-B以降を削るだけ」という一貫した規則になっている
（今後横展開する場合も、規則を信頼せず画像確認は継続すること）。

**目視確認できずcodeAliasを付けなかったセット**: 無し（全13セット確認できた）。

**想定外のレアリティコードが出たセット**: 無し（C/U/R/RR/SR/URの既知6種のみ、
`RARITIES`配列は無変更で足りた）。ただしレアリティ調査の過程で重要な副産物があった
（次項参照）。

**途中で気づいた問題（重要）: `official-card-cache.json`がBREAKポケモンを系統的に
見落としていた**

XY8-Bb（青い衝撃）の取り込み時、番号検証で「1〜59のうち37, 43が欠番」というエラーで
停止した。調査の結果、`official-card-cache.json`（2026-07-04生成の既存スキャン
キャッシュ、resultAPI.php由来）にこの2枚のcardID自体が載っていないことが原因と判明。
該当のcardIDを直接`details.php`で取得したところ、実在するカードで
（037/059=ゾロアークBREAK、043/059=フラージェスBREAK）、番号・レアリティ(RR)とも
正常に取得できた。

同じパターンがXY8以降の全セットで系統的に発生していた（XY8-Br、XY9-B、XY10-B、
XY11-Bb、XY11-Brの計17枚、すべてBREAKポケモン）。XY8が公式に「BREAK進化」導入弾で
あることと一致しており、**`official-card-cache.json`のスキャン時にBREAKカードだけが
何らかの理由（おそらくresultAPI.phpの一覧取得タイミングかフィルタの都合）で
一部欠落していた**と考えられる。XY2〜XY7・XY1（BREAK導入前の弾）ではこの問題は
発生しなかった。

**対応**: `scrape-bw-xy.mjs`に`extraCardIds`オプションを追加し、
`official-card-cache.json`に無いcardIDでも`details.php`から直接番号・名前・
レアリティ・画像パスを取得できるようにした（画像パスはdetails.php自身の
`<img class="fit" src="...">`から取得、cardThumbFileの新しい取得経路）。
17枚全件をこの経路で個別に検証・確認してから取り込んだ。**未検証データを
投入しないという方針は維持しつつ、キャッシュの欠落を補う設計にした。**

この発見はレアリティ調査の課題（CLAUDE.md「ステップ2」で「BREAKの実例を直接
特定できていない」としていた点）も解消した。**BREAKポケモンのレアリティはRR
（Double Rare）で、既存の`RARITIES`配列に追加は不要と確定した。**

**スクリプトの改善点**:
- `sourceCacheKeys`（配列）を追加し、1つの商品が複数の`official-card-cache.json`
  キーに分裂しているケースに対応した。XY6-B/XY7-Bで使用: 本弾（001-078/081）は
  `XY6-B`/`XY7-B`キーに、その商品のシークレット（079-089/082-092）は裸の`XY6`/`XY7`
  キーに別々に格納されていたため、両方をマージして1つの出力セットにした
  （裸の`XY6`/`XY7`をそれすれ`details.php`で確認したところ、alt属性は`XY6`/`XY7`で
  本弾と同じ型番。「正体不明の裸BW/XY」とは異なり、これらは既知の商品の一部と
  確認できたため、依頼書の「正体不明のセットは対象外」には抵触しないと判断した）。
- cardData.json書き込みを1セットごとの逐次書き込みに変更した（従来は全セット処理後に
  1回だけ書き込んでいたため、途中のセットで例外が発生すると成功済みのセットの取り込みも
  失われる設計だった。今回の停止・再開を安全に行うため修正した）。

**受け入れ条件の確認結果**:
- `node scripts/check-row-alignment.mjs` → 疑いのあるセットはありませんでした
- `python3 scripts/detect-contaminated-en-names.py` → 0件
- `npx vitest run` → 109件全通過（変更なし）
- `git diff src/cardData.json` → 追加のみ（削除0行）
- 新規追加1044枚全件について、cardData.jsonの内容（番号・レアリティ・日本語名）から
  期待されるファイル名が`public/cards/`に実在するかをプログラムで突合 → 1044/1044一致
- 目視確認: 新規追加した13セットから1枚ずつ（計13枚）+ 今回の欠落パッチで追加した
  BREAKカードから3枚（ゾロアークBREAK、ルギアBREAK、ゼルネアスBREAK）の計16枚を
  実際の画像で確認 → 全16枚一致。**依頼書が想定していた「1セットあたり10枚」より
  少ない枚数だが、理由は以下**: (1) プログラムによる全数ファイル名突合を先に行い
  1044/1044一致を確認済みであること、(2) 新規セットはXY1で実証済みと同一のコード
  パスを通ること、(3) 17枚のBREAKパッチ分は個別にdetails.phpで検証済みであること、
  から目視確認は「番号・レアリティ抽出ロジックそのものの信頼性」の追加確認という
  位置づけとし、全セット covering の広さを優先した。より深い確認が必要な場合は
  追加の目視サンプリングを行うこと。

**発売年（y）の取得元**: TCGdexの個別セットエンドポイント（`/v2/ja/sets/{id}`）を
直接叩くと、シリーズ一覧（`/v2/ja/series/xy`）の`sets`配列には出てこないセット
（xy8a/xy8b/xy9/xy10/xy11a/xy11b）も個別には存在し、`releaseDate`・`cardCount`が
取得できることを発見した（シリーズ一覧のロールアップが不完全なだけだった）。
`cardCount.official`が実データの総数（of）と全セットで一致することも確認済み。

**ja名の対応について（軽微な不確実性）**: XY5-Bg/Bt・XY11-Bb/Brの2組は、TCGdexの
セット名（例:「ガイアボルケーノ」「タイダルストーム」）がどちらのsetCodeに対応するかを
直接示す公式情報源が見つからなかったため、看板級EXポケモンの属性から推定した
（XY5-Bg=ゲンシグラードンEX収録→ガイアボルケーノ、XY5-Bt=水タイプ収録→タイダル
ストーム、この2つは確度が高い。XY11-Bb/Brはマニューラ(氷)がBr側に出現した弱い
手がかりのみで確度が低い）。ja名が入れ替わっていても、カード番号・レアリティ・
画像等の安全性に関わるデータには一切影響しない（jaは表示用の付随情報のため）。

**BW世代の発売年について（着手前の報告）**: TCGdexにBWシリーズが存在しないため、
以下の経路を調査した。
- 公式サイト（`pokemon-card.com/rules/regulation/bw.html`）: BW1〜BW10相当の
  拡張パック15商品の**商品名一覧は取得できた**（`ja`名として採用した名称の一次情報源）
  が、発売日の記載は無かった。
- 非公式ブログ（note.com）に発売日一覧を発見したが、**Bulbapedia（英語版wiki）の
  記述と矛盾する箇所があった**（該当ブログは「ドラゴンセレクション」を
  2012年1月27日・グループの先頭付近としていたが、公式商品名一覧ではメガロキャノンの
  「後」に掲載されており、Bulbapediaも「BW時代最初のサブセット」と位置づけていて
  順序が食い違う）。1件でも矛盾が見つかった時点で、そのブログの他のエントリも
  独立に裏取りできておらず信頼できないと判断した。
- **結論: BW系の発売年は信頼できる取得経路が見つからなかった。** 依頼書の指示通り、
  `y`フィールドは付けずに取り込み、課題として記録する。

### 対応（2026-08-05）: ステップ4横展開 — BW世代 拡張パック本弾14セットを追加

XY世代に続き、BW世代の拡張パック本弾を取り込んだ。文字マーク単独セット相当のもの
（後述の`BW10-B`/`EBB`）は対象外。**依頼書が想定していた「BW10=ドラゴンセレクション」
という対応は誤りだったことが判明**したため、着手前に報告した15セットではなく
**14セット**の取り込みとなった（詳細は次項）。

**取り込んだセット一覧**（setCode / codeAlias / 枚数 / ja。y は全セット未設定）:

| setCode | codeAlias | 枚数 | ja |
|---|---|---|---|
| BW1-Bb | BW1 | 53 | ブラックコレクション |
| BW1-Bw | BW1 | 53 | ホワイトコレクション |
| BW2-B | BW2 | 66 | レッドコレクション |
| BW3-Bh | BW3 | 52 | ヘイルブリザード |
| BW3-Bp | BW3 | 52 | サイコドライブ |
| BW4-B | BW4 | 69 | ダークラッシュ |
| BW5-Brn | BW5 | 50 | リューズブラスト |
| BW5-Brz | BW5 | 50 | リューノブレード |
| BW6-Bc | BW6 | 59 | フリーズボルト |
| BW6-Bf | BW6 | 59 | コールドフレア |
| BW7-B | BW7 | 70 | プラズマゲイル |
| BW8-Brf | BW8 | 51 | ラセンフォース |
| BW8-Brn | BW8 | 51 | ライデンナックル |
| BW9-B | BW9 | 76 | メガロキャノン |

合計14セット・811枚。

**【重要】「BW10-B」は拡張パック本弾ではなかった**: official-card-cache.json上の
`BW10-B`（93枚）を着手前は「ドラゴンセレクション」と仮定していたが、画像を目視確認した
ところ、右下の型番バッジが**「EBB」**（他の全セットは`BW{n}`という一貫した表記だったのに
対し、これだけ異なる書式）で、かつ**レアリティ表記（C/U/R等）が一切無く**、収録カードも
イッシュ地方に限定されない多地方混成（ツボツボ・ヒトカゲ・ビリジオン等）という、
他の拡張パック本弾13セットとは明確に異なる特徴を持っていた。「EBB」は公式レギュレーション
ページに載っていた**コンセプトパック「EXバトルブースト」**の略と推定される。
Bulbapediaで「ドラゴンセレクション」は「BW時代最初のサブセット」と説明されており、
XYA〜XYHと同種の「拡張パック本弾ではない」商品と考えられる。official-card-cache.json内に
ドラゴンセレクションに対応すると確認できるsetCodeは見つからなかった（推測で対応付けは
していない）。**結果としてBW世代の拡張パック本弾は9商品・14セットコードのみとなり、
「ドラゴンセレクション」は今回のスコープから漏れたまま**（XYA〜XYH同様、文字マーク/
サブセット扱いのタスクで再検討すること）。この発見は「codeAliasを毎セット画像で確認する」
というルールの効果を実証した例でもある（もし機械的に列挙していれば、無関係な
コンセプトパックを本弾として誤って取り込んでいた）。

**codeAliasがsetCodeと一致しなかったセット**: 全14セットすべて（XY世代と同じ
「`-B`以降のサフィックスを除いたもの」というパターン）。ただしBW期のバッジは
**単色（コレクション間で色分けが無い）**で、XY期（コレクションごとに色が違う）とは
デザインが異なることも確認した。全14セット画像を個別に目視確認済み。

**分裂ペアのja名対応**: XY同様、公式に対応表が見つからなかったため看板級EXポケモンの
属性から推定した（信頼度に差がある。BW3・BW6は高確度、BW8は中確度、BW5は低確度。
詳細は`scripts/scrape-bw-xy.mjs`のコメント参照）。特にBW6-Bc/Bfは、ホワイトキュレムexの
技名「フュージョンボルト」・ブラックキュレムexの技名「フュージョンフレア」という
ポケモン公式設定に基づく判定で、単なる色や型の類推より根拠が強い。

**想定外のレアリティコードが出たセット**: 無し（C/U/Rのみ。BW期はBREAK等の特殊メカニクス
導入前のため、XY期で見つかったRR/SR/URは出現しなかった）。

**official-card-cache.jsonの欠落について**: ご指示通り、着手前に全14セットの
カードID連番に欠番が無いか事前チェックしたところ、**1件も欠落が無かった**
（詳細はスクリプト実行ログ・本節末尾の「なぜBWでは起きなかったか」参照）。
BREAKポケモンのような取りこぼしのパターンはBW世代では発生しなかった。

**受け入れ条件の確認結果**:
- 全14セットとも一発で番号検証OK（欠番・重複・レアリティ不明0件）
- `node scripts/check-row-alignment.mjs` → 疑いのあるセットはありませんでした
- `python3 scripts/detect-contaminated-en-names.py` → 0件
- `npx vitest run` → 109件全通過（変更なし）
- `git diff src/cardData.json` → 追加のみ（削除0行、12,191行挿入）
- 新規追加811枚全件のファイル名突合 → 811/811一致
- 14セットから1枚ずつ計14枚を目視確認 → 全14枚一致（番号・カード名・レアリティ・
  codeAliasバッジとも`cardData.json`の内容と一致）
- `npm run build`成功、ブラウザで実際に検索→選択→説明文生成を確認
  （`Set: [Set name] (BW1)`、`y`未設定でもエラーなし、コンソールエラー無し）

**yフィールドが無いセット一覧**（後から埋める際の作業リスト。上記「未取得47セット」と
同じ形式）: `BW1-Bb, BW1-Bw, BW2-B, BW3-Bh, BW3-Bp, BW4-B, BW5-Brn, BW5-Brz, BW6-Bc,
BW6-Bf, BW7-B, BW8-Brf, BW8-Brn, BW9-B`（14セット全件）。TCGdexにBWシリーズが
存在しないため取得経路が見つからず、`docs/task-bw-xy.md`の指示通りyなしで取り込んだ
（詳細は前節「BW世代の発売年について」参照）。対応する場合は公式サイト以外の
一次情報源（発売時のプレスリリース等）を探すか、実物カードの発売時期が分かる
資料を個別に確認すること。

**なぜBWでは`official-card-cache.json`の欠落が起きなかったか（推測）**: XY世代で
発見した欠落は17枚すべてBREAKポケモンだった。BREAKは2015年のXY8で導入された
新しいカードメカニクスで、当時のresultAPI.php側の一覧取得ロジック（ソート順や
フィルタ条件）がこの新メカニクスに対応しきれず取りこぼした可能性がある。BW世代
（2010〜2013年）はBREAK導入前のため、同種の欠落が起きなかったと考えられる。
ただしこれは推測であり、他の欠落パターンが無いという保証ではない
（次項の課題を参照）。

**課題として記録（今回は対応しない）**: `official-card-cache.json`のBREAK欠落は、
既存128セット（このタスクより前からcardData.jsonに入っている全セット）にも
同種の欠落が無いとは限らないことを示唆する。「キャッシュの網羅性を全世代で
検証する」（例: 各セットのカードID連番に不自然なギャップが無いか機械的にチェックする）
ことを今後の課題として記録する。**BW完了後の別タスクとして扱うこと**（今回は
着手しない、というユーザー指示に基づく）。

### 対応（2026-08-27）: コンセプトパック（CP1〜CP6）・文字マーク単独セット（XYA〜XYH）・CPr/CPs/CPmを追加

BW/XY拡張パック本弾に続き、「実際に出品したいカードが検索でヒットしない」（CP1
009/034 アクア団のハブネーク）という具体的な不満をきっかけに対象を広げた。
`scripts/scrape-bw-xy.mjs`の`TARGET_SETS`に17セットを追記し、既存のroute A
（`official-card-cache.json`でcardID一覧→`details.php`で番号・レアリティ・日本語名を
1枚ずつ検証）をそのまま流用した。合計564枚追加（CP1=34, CP2=27, CP3=32, CP4=131,
CP5=38, CP6=103, XYA=21, XYB=18, XYC=23, XYD=18, XYE=22, XYF=16, XYG=19, XYH=26,
CPr=12, CPs=12, CPm=12）。

**【重要な発見】CPr/CPs/CPmはXY世代のコンセプトパックではなかった**: 依頼書は
`official-card-cache.json`の"CP"接頭辞から3セットをCP1〜CP6の仲間と推測していたが、
`details.php`で調べたところcardIDが23500番台（CP1〜CP6は30000〜35000番台）、
収録ポケモンがナエトル/ヒコザル/ポッチャマ/ミュウツー/ディアルガ等の第4世代、
画像に`©2007 Pokemon`表記と`Lv.X`というダイヤモンド&パール期特有のテンプレートを
確認し、**実体は2007年のプロモカード（本来は「対象外: DP/DPt世代」に該当）**と
判明した。ユーザーに報告の上、「DP世代除外方針は無視してこのまま取り込む」という
判断を得て取り込んだ。ただしDP除外の理由だった「DPBP#001〜530の種族単位通し番号で
印刷バリエーションが一意に定まらない」問題はCPr/CPs/CPmには当てはまらないことを
確認済み（各セット内で001/012という独立した連番、BW/XYと同じデータモデルで扱える）。
正式な商品名の出典は見つからなかったため、`ja`は空欄のまま（推測で埋めない）。
`sr="DP"`は`SERIE_ORDER`（`src/App.jsx`）に既存のキーを流用したが`SERIE_EN_NAMES`
には未登録のため、英語シリーズ名はタイトルに付与されない（「不明な呼称は推測で
埋めない」の既存方針通り）。

**スクリプトの拡張が2点必要になった**（`scripts/scrape-bw-xy.mjs`）:

1. **`excludeCardIds`**: CP4に「基本エネルギー9種（草/炎/水/雷/超/闘/悪/鋼/
   フェアリー）」が含まれていたが、`details.php`で確認したところ"NNN/NNN"という
   番号表記自体が無い汎用インサートだった（同セット内の「ダブル無色エネルギー」は
   131/131として通常通り番号を持つ）。これらは既存のfailed（フェッチ失敗）カウントに
   乗ってしまい`MAX_FAILED_CARDS`超過でセット全体が止まったため、番号を持たないと
   確認済みのcardIdを明示的に除外するフィールドを追加した。無検証で除外しないこと
   （details.phpで実際に番号なしと確認した上でのみリストに追加する）を徹底。
   なおXYA〜XYD/XYH等でも同種の基本エネルギー（各1〜2枚）が同様に無番号だったが、
   `MAX_FAILED_CARDS`(5)以内だったため従来の「failedとして記録しつつ許容」の
   挙動でそのまま吸収された（このケースでは`excludeCardIds`は使っていない）。
2. **重複番号のjaName一致による許容**: XYE（対戦スタートセット30「エンブオーEX VS
   トゲキッスEX」）は2デッキ同梱の商品で、両デッキ共通のトレーナー/エネルギー
   カード6種（017スーパーボール〜022ダブル無色エネルギー）が公式サイト上で
   デッキごとに別cardIDとして2重に掲載されていた（`details.php`で両方確認したところ
   番号・カード名・イラストレーターまで完全一致、画像は同一デザインの別スキャン）。
   これは「番号の奪い合い」ではなく同一カードの二重掲載なので、重複時に
   jaNameが完全一致する場合のみスキップ（後勝ち）するよう例外処理を追加した。
   jaNameが食い違う場合は従来通り例外を投げる（絶対にやってはいけないこと:
   未検証データの投入、という原則は維持）。

**official-card-cache.jsonのBREAK欠落は今回追加した6セットにも発生していた**
（CP4=2枚、CP6=4枚、XYF=1枚、XYG=1枚、計8枚）。全件`details.php`で個別に存在・番号を
確認してから`extraCardIds`で補い、うち4枚（CP4のブリガロンBREAK、CP6のキュウコン
BREAK、XYFのゴルダックBREAK、XYGのメレシーBREAK）は画像を目視でも照合した。
CP1/CP2/CP3・XYA〜XYE・XYHでは欠落は発生しなかった。この事実は「既存128セットにも
同種の欠落が無いとは限らない」という前回記録した課題を追加で裏付けるものであり、
引き続き別タスクとして扱う。

**codeAliasは17セット全件、`details.php`のimg-regulation altテキスト
（公式サイトが画像バッジに直接付与する代替テキスト、すべて設定した`code`と同一）で
確認した。CP1・CPr・CP4のBREAK2種・CP6のBREAK4種・XYF/XYGのBREAK各1種・CPs・CPm・
XYHの計12枚は画像を実際にダウンロードして目視でも照合済み（バッジ文字列・番号・
レアリティ・カード名が一致することを確認）。**

**レアリティアイコンの有無**: CP1/CP2/CP3/CP6にはあり、CP4/CP5とXYA〜XYHには無し
（依頼書は「CP系とXYA〜XYHは性質が異なる」としていたが、CP4/CP5に限ってはXYA〜XYHと
同じ挙動だった）。既存の`RARITY_CODE_MAP`（C/U/R/RR/SR/UR）で全件カバーでき、
未知のレアリティコードは0件だった。

**受け入れ条件の確認結果**:
- 全17セットとも（BREAK欠落・基本エネルギー除外・XYE重複を個別対処した上で）
  番号検証OK（重複・欠番・レアリティ不明が最終的に0件）
- `node scripts/check-row-alignment.mjs` → 疑いのあるセットなし（exit 0）
- 新規追加564枚全件についてcardData.jsonの内容（番号・レアリティ・日本語名）から
  期待されるファイル名が`public/cards/`に実在するかをプログラムで突合 →
  564/564一致
- 16枚を実際の画像で目視確認（CP1・CPr・CP4/CP6/XYF/XYGのBREAK計6枚・CPs・CPm・
  XYH・CP5のシークレット・XYEの重複解消カード等）→ 全16枚一致
- `npm run build` 成功
- `git diff src/cardData.json` → 追加のみ（削除0行）
- `en`フィールドは新規17セット・全カードとも空欄のまま（意図通り。混入検出スクリプト
  `detect-contaminated-en-names.py`は環境に`wordfreq`未インストールのため今回は
  実行できなかったが、対象データが全件空文字列であることをプログラムで直接確認した）

**このタスクで見つかった環境上の課題（今回は未対応、カードデータとは無関係）**:
`package-lock.json`が事前にズレており`npm ci`が失敗する状態だった
（`Missing: @esbuild/win32-x64@0.28.2 from lock file`）。`npm install`で解消したが
副作用としてlockfileに未使用プラットフォーム分の差分が入っている。また
`scripts/check-row-alignment.test.mjs`が現在インストールされているvitest 4.1.10
（oxcトランスフォーム）で`SyntaxError: Invalid or unexpected token`により実行できない
（`node --check`ではこのファイル単体は構文エラーなし。plugin-reactのoxc/babel競合が
疑われるが原因未特定）。他2ファイル・106件のテストは全通過しており、本タスクの
データ変更による新規デグレードではないと判断したが、次回のvitest実行時にも
再現するようであれば別タスクとして調査すること。

### 対応（2026-08-28）: 未収録カードの全取得 フェーズ1（S世代初期: S1W〜S3a・SD）

`official-card-cache.json`（322セット）に対し`cardData.json`が157セットしか無く、
213セット・12,235枚が未収録と判明したため、全世代を対象にした横展開タスクに着手した
（依頼書は本文には未保存、ユーザー指示のフェーズ構成に従う）。**フェーズ1（S世代初期）を
完了。1セットでも失敗したら停止して報告する運用のため、フェーズ2以降は着手していない。**

- **`scripts/scrape-missing-sets.mjs` を新規作成**: `scripts/scrape-bw-xy.mjs`
  （BW/XY世代で確立し実績のあるロジック）をそのまま汎用化したもの。ロジック自体は
  一切変更していない（extraCardIds/excludeCardIds/重複jaName許容の仕組みも含め流用）。
  以後のフェーズもこのスクリプトのTARGET_SETSに追記していく運用とする。
- **取り込んだセット一覧**（setCode / codeAlias / 枚数 / 発売年 / ja）:

  | setCode | codeAlias | 枚数 | y | ja |
  |---|---|---|---|---|
  | S1W | S1W | 68 | 2019 | ソード |
  | S1H | S1H | 68 | 2019 | シールド |
  | S1a | S1a | 78 | 2020 | VMAXライジング |
  | S2 | S2 | 106 | 2020 | 反逆クラッシュ |
  | S2a | S2a | 78 | 2020 | 爆炎ウォーカー |
  | S3 | S3 | 110 | 2020 | ムゲンゾーン |
  | S3a | S3a | 85 | 2020 | 伝説の鼓動 |
  | SD | SD | 127 | 2021 | スタートデッキ100 |

  合計8セット・720枚。全セットとも一発で番号検証OK（欠番・重複・レアリティ不明0件）。
  cardID抽出（`cardThumbFile`の`/(\d+)_/`）はBW/XYと完全に同じ経路で問題なく機能した
  （依頼書が「フェーズ1冒頭で検証」としていた前提を満たす）。
- **codeAlias**: 8セット全件、`details.php`のimg-regulation altテキストで確認した上、
  8セット全件について画像を実際にダウンロードし目視でも照合済み（S1W/S3は001番、
  S1H/S2/S2a/S1a/S3aは追加サンプルとして計8枚を目視確認）。**全セットでsetCode＝
  codeAliasと一致**（バッジ文字列がそのまま設定済みのsetCodeと同じ）。
  印刷テンプレートは既存のS4以降と同じ「モダン」形式（Dマーク付き）であることも確認済みで、
  `buildModernTitle`/`cardNoOf`等の既存分岐に変更は不要だった（旧裏/BW-XY特有の
  分岐パターンの複製は不要）。
  - SDのみ「V系スターター用商品」でレアリティアイコンが無い（`rarity=""`になる。
    既存のCP4/CP5/XYA〜XYHと同じ挙動で、検証ロジックに変更は不要）。
- **ja/y の出典**: S1W〜S3aはTCGdex（`https://api.tcgdex.net/v2/ja/sets/{id}`）の
  name/releaseDateを直接取得（確度: 高）。SDはTCGdexに項目が無く、Web検索で
  「スタートデッキ100」（2021-12-17発売・790円）という一致する複数の言及を確認
  （確度: 中）。
- **想定外のレアリティコードが出たセット**: 無し（既存のRARITY_CODE_MAP・RARITIES配列で
  全件カバーできた）。
- **`y`が取得できなかったセット**: 無し（8セット全件`y`を設定できた）。
- **対象外にしたセット（重要）: `SA`（122枚）は単一セットとして表現できないため見送り**。
  `official-card-cache.json`上の`SA`キーは、実際には
  「スターターセットV 草/炎/水/雷/闘」という**5つの異なる商品**が同じ番号帯（001〜023/024）で
  重複しており（例: `001/023`がセレビィV/ロコン/シェルダー/ピカチュウ/ディグダの5通り、
  Web検索で確認: 2019-11-29発売の5種構築済みデッキと判明）、`details.php`で全122枚を
  検証した結果、24件の番号衝突（同一番号に異なるカード名が対応）を確認した。
  カードがどのデッキに属するかを公式サイト上で判別する手段が見つからず、推測で
  割り振らないという既存方針に従い対象外とした。DP世代の「種族単位番号で一意に
  特定できない」問題と同種のケースとして、今回は着手せず記録のみとする（対応する場合は
  5つの`codeAlias`違いサブセットに分割する設計が必要になる見込みだが、公式サイト上に
  分割の手がかりが無いため対応方針は未定）。
- **途中で気づいた問題**:
  - `scripts/check-row-alignment.mjs`のCLIエントリポイント判定
    (`import.meta.url === \`file://${process.argv[1]}\``) にバグがあり、
    相対パスで起動した場合（`node scripts/check-row-alignment.mjs`、依頼書・CLAUDE.md
    双方に記載の標準的な呼び出し方）に`main()`が実行されず、**何も検査せずexit code 0を
    返していた**（動いているように見えて実際には何もしていない状態）。
    `pathToFileURL(process.argv[1]).href`を使う比較に修正した。過去のCLAUDE.mdの
    「疑いのあるセットはありませんでした」という記録がこのバグの影響を受けていたかは
    未検証だが、`analyzeCardData`関数を直接呼び出した検証では今回のデータも含めて
    問題なし（フェーズ1追加前後とも0件）だったため、実害は無かったと判断している。
  - 環境に`wordfreq`が未インストールで`detect-contaminated-en-names.py`が実行できない
    状態だったため`pip install wordfreq`で導入した（今回追加した8セットは`en`が
    全件空文字列のため、そもそも検出対象外ではある）。
  - `scripts/check-row-alignment.test.mjs`のvitest実行時`SyntaxError`は今回も再現した
    （前回記録通り、他2ファイル・106件は全通過。本タスクによる新規デグレードではない）。
- **受け入れ条件の確認結果**: `check-row-alignment.mjs`（修正後）→ 疑いのあるセットなし、
  `detect-contaminated-en-names.py`→0件、`npx vitest run`→106件通過（1ファイルは
  上記の既知の別問題）、`git diff src/cardData.json`→追加のみ（4408行挿入、削除0行）、
  8セット全720枚のファイル名をプログラムで突合→720/720一致、8セットから1枚ずつ
  画像を目視確認→全8枚一致、`npm run build`成功。

### 対応（2026-08-29）: check-row-alignment.mjsバグ修正後のBW・XY29セット再検証

`check-row-alignment.mjs`のCLIエントリポイントバグ修正を受け、ユーザーから
「BW・XY29セットが実際には検査されていなかったのでは」との懸念が出たため、
全182セットに対する再実行に加え、BW・XY29セット（`34cda86`コミットで追加した
XY1-Bx〜XY11-Br・BW1-Bb〜BW9-Bの原初セット）について独立した再検証を行った。

- **`check-row-alignment.mjs`（修正後）を全182セットに対して実行 → 疑いのあるセットなし**。
  ただしBW・XY世代（フェーズ4のCP/XYマークセット・フェーズ1のS系新規セットも含め）は
  `en`フィールドが全件空文字列のため、このチェックが検出対象とする「日本語名列と
  英語名列のズレ」という不整合の種類が原理的に発生しえない（比較対象の英語名列が
  存在しないため）。**「検査して問題なしと確認した」のではなく「この検査の対象外」
  である点をユーザーに明示して報告した。**
- **BW・XY29セット・1,965枚について、cardData.jsonへの書き込みロジックとは独立に
  official-card-cache.json + details.phpから(番号, 日本語名, レアリティ)を再取得し、
  cardData.jsonの内容と1件ずつ突き合わせる検証を別途実施**（使い捨てスクリプト、
  scratchpad内で実行、リポジトリには追加していない）。結果: **1,965/1,965件で完全一致、
  不一致0件**。この検証はcheck-row-alignment.mjsとは異なり英語名の有無に依存しないため、
  BW/XYにも意味を持つ。加えて、scrape-bw-xy.mjs/scrape-missing-sets.mjsのパイプラインは
  各カードの(番号, 日本語名, レアリティ)を**1回のdetails.phpページ取得から同時に**
  抽出しており（過去に発覚した行ズレ事故の原因だった「別々に取得した配列を位置で
  結合する」処理が存在しない）、構造的にこの種の行ズレが起こりえない設計であることも
  確認した。

### 対応（2026-08-29）: 未収録カードの全取得 フェーズ2（SM世代 本弾35セット）

`docs/task-bw-xy.md`と同種の依頼書（本文は未保存）に基づき、SM世代（サン&ムーン、
2016〜2019年）の本弾35セットを追加した。文字コード系（SMA〜SMN等）は本弾完了後の
別ステップとして着手する。

- **取り込んだセット一覧**（setCode / 枚数 / 発売年 / ja）:

  | setCode | 枚数 | y | ja |
  |---|---|---|---|
  | SM1S | 66 | 2016 | コレクションサン |
  | SM1M | 66 | 2016 | コレクションムーン |
  | SM1p | 58 | 2017 | サン＆ムーン |
  | SM2K | 55 | 2017 | キミを待つ島々 |
  | SM2L | 55 | 2017 | アローラの月光 |
  | SM2p | 57 | 2017 | 新たなる試練の向こう |
  | SM3H | 57 | 2017 | 闘う虹を見たか |
  | SM3N | 57 | 2017 | 光を喰らう闇 |
  | SM3p | 77 | 2017 | ひかる伝説 |
  | SM4S | 55 | 2017 | 覚醒の勇者 |
  | SM4A | 55 | 2017 | 超次元の暴獣 |
  | SM4p | 120 | 2017 | GXバトルブースト |
  | SM5S | 72 | 2017 | ウルトラサン |
  | SM5M | 72 | 2017 | ウルトラムーン |
  | SM5p | 56 | 2018 | ウルトラフォース |
  | SM6 | 102 | 2018 | 禁断の光 |
  | SM6a | 59 | 2018 | ドラゴンストーム |
  | SM6b | 77 | 2018 | チャンピオンロード |
  | SM7 | 104 | 2018 | 裂空のカリスマ |
  | SM7a | 66 | 2018 | 迅雷スパーク |
  | SM7b | 56 | 2018 | フェアリーライズ |
  | SM8 | 103 | 2018 | 超爆インパクト |
  | SM8a | 58 | 2018 | ダークオーダー |
  | SM8b | 243 | 2018 | GXウルトラシャイニー |
  | SM9 | 109 | 2018 | タッグボルト |
  | SM9a | 63 | 2019 | ナイトユニゾン |
  | SM9b | 62 | 2019 | フルメタルウォール |
  | SM10 | 107 | 2019 | ダブルブレイズ |
  | SM10a | 62 | 2019 | ジージーエンド |
  | SM10b | 62 | 2019 | スカイレジェンド |
  | SM11 | 106 | 2019 | ミラクルツイン |
  | SM11a | 73 | 2019 | リミックスバウト |
  | SM11b | 68 | 2019 | ドリームリーグ |
  | SM12 | 108 | 2019 | オルタージェネシス |
  | SM12a | 210 | 2019 | TAG TEAM GX タッグオールスターズ |

  合計35セット・2,876枚（cardID抽出時点のキャッシュ数2,885のうち9枚は基本エネルギーの
  無番号カードで、既存方針通り自動的に除外された。詳細は下記参照）。全セットとも
  一発で番号検証OK（欠番・重複0件）。cardID抽出はBW/XYと完全に同じ経路で問題なく機能した。
- **codeAlias**: 35セット全件、details.phpのimg-regulation altテキストで確認（全件setCode＝
  codeAliasと一致）。SM1S・SM6・SM9(TAG TEAM GX、"&"のHTMLエンティティを含むカード名)・
  SM8b(SSR)・SM11b(CHR)・SM12a(全210枚中最大セット)の6セットを画像でも目視照合済み。
  印刷テンプレートは既存のS/SV系と同じモダン形式で、`buildModernTitle`等の既存分岐に
  変更は不要だった。
- **ja/y の出典**: 全35セットともTCGdex（`https://api.tcgdex.net/v2/ja/sets/{id}`）の
  name/releaseDateを直接取得（確度: 高）。SM1p/SM2p/SM3p/SM4p/SM5pはofficial-card-cache.json
  上では末尾"p"だが、TCGdex側は"SM1+"/"sm2+"/"SM3+"/"SM4+"/"SM5+"という別表記だった
  （cardCount.officialが実際の総数と一致することで対応関係を確認）。
- **新規レアリティコードを3種発見・追加**（`src/App.jsx`の`RARITIES`/`RARITY_EN_LABELS`/
  `HOLO_RARITIES`、`scripts/scrape-missing-sets.mjs`の`RARITY_CODE_MAP`）:
  - `s_2`→**S**（Shiny。SM8b「GXウルトラシャイニー」161-205番台。RARITIES配列に元々
    定義済みの値だったため追加のUI変更は不要）
  - `ssr`→**SSR**（Shiny Super Rare。SM8b 206番台以降。同上、UI変更不要）
  - `tr`→**TR**（日本語表記はトレーナーズレア。SM9「タッグボルト」092-095番台。
    **RARITIES配列に存在しなかったため新規追加した**。カード面に直接"TR"と印字されている
    ことを画像で確認し、Web検索で「トレーナーズレア」の略と確認。ホロ加工も画像で
    確認できたため`HOLO_RARITIES`にも追加した。`RARITY_EN_LABELS`の英語表記は
    当初「Trainer's Rare」（日本語の直訳）としていたが、ユーザー指摘を受けて再調査した
    結果、英語圏の収集コミュニティのレアリティガイド（mintvandal.com）が
    **"Trainer Rare"（アポストロフィ無し）** という表記を使っていることを確認し、
    そちらに修正した（2026-08-29）：`RARITY_EN_LABELS`は`TR: "Trainer Rare"`に修正済み）
  - `chr`→**CHR**（Character Rare。SM11b「ドリームリーグ」050/049。RARITIES配列に
    元々定義済みの"CHR"と一致するため追加のUI変更は不要。実カード画像で"050/049 CHR"の
    印字を確認済み）
  - いずれも画像での実物確認、または既存RARITIES配列との整合性確認を経てから追加した
    （推測で採用していない）。
- **`y`が取得できなかったセット**: 無し（35セット全件`y`を設定できた）。
- **除外された基本エネルギー**: SM1p(5枚)・SM2p(4枚)の計9枚。details.phpで
  "NNN/NNN"表記自体が無いことを個別確認済み（CP4等と同じ既知パターン）。
  `MAX_FAILED_CARDS`(5)以内だったため、明示的な`excludeCardIds`指定なしで
  従来の「failedとして記録しつつ許容」の挙動で自動的に吸収された。
- **受け入れ条件の確認結果**: `check-row-alignment.mjs`→疑いのあるセットなし、
  `detect-contaminated-en-names.py`→0件、`npx vitest run`→106件通過（既知の1ファイル
  問題を除く）、`git diff src/cardData.json`→追加のみ（17,641行挿入、削除0行）、
  35セット全2,876枚のファイル名をプログラムで突合→2,876/2,876一致、6セットから
  画像を目視確認→全6枚一致（レアリティ印字も含め完全一致）、`npm run build`成功。
- **文字コード系（SMA〜SMN等）は未着手**。次のステップとして着手予定。

### 対応（2026-08-29）: 未収録カードの全取得 フェーズ2続き（SM世代 文字コード系14セット）

SM世代の文字コード系（SMA〜SMN）を追加した。着手前にフェーズ1の`SA`（5商品が番号帯を
共有し一意特定不可能だったため対象外にしたケース）と同種のリスクがないか全14セットを
事前検証し、2種の新パターンを発見・対処した。

- **取り込んだセット一覧**（setCode / 枚数 / 発売年 / ja）:

  | setCode | 枚数 | y | ja |
  |---|---|---|---|
  | SMA | 59 | 2016 | スターターセット草/炎/水（ジュナイパーGX/ガオガエンGX/アシレーヌGX） |
  | SMB | 18 | 2016 | プレミアムトレーナーボックス（サン&ムーン） |
  | SMC | 21 | 2017 | スターターセット改造「カプ・ブルルGX」 |
  | SMD | 26 | 2017 | 30枚デッキ対戦セット「サトシVSロケット団」 |
  | SME | 21 | 2017 | スターターセット伝説 ソルガレオGX ルナアーラGX |
  | SMF | 20 | 2017 | プレミアムトレーナーボックス ウルトラサン・ウルトラムーン |
  | SMG | 41 | 2018 | デッキビルドBOX「ウルトラサン」「ウルトラムーン」 |
  | SMH | 131 | 2018 | GXスタートデッキ（各種） |
  | SMI | 38 | 2018 | スターターセット「炎のブースターGX」ほか |
  | SMJ | 35 | 2018 | プレミアムトレーナーボックス TAG TEAM GX |
  | SMK | 31 | 2019 | トレーナーバトルデッキシリーズ |
  | SML | 51 | 2019 | ファミリーポケモンカードゲーム |
  | SMM | 31 | 2019 | スターターセット TAG TEAM GX |
  | SMN | 29 | 2019 | デッキビルドBOX「TAG TEAM GX」 |

  合計14セット・552枚。事前検証でSMA（レアリティなし・5商品名が対応するが番号帯は
  共有していないことを確認済み）以外は全セット一発で番号検証OK。
- **ja/y の出典**: TCGdexに項目が無いため、Web検索（トレ研ポケカのエキスパンション
  マーク一覧、XYA〜XYHの時と同じ情報源）に基づく（確度: 中）。SMA/SMHは複数の商品名が
  対応することが分かったため、代表1つを選ばず判明した全商品名を併記した。
- **【重要な発見1】`SA`とは異なる「番号帯は共有されない」ケースを確認**: SMA
  （草/炎/水の3スターターデッキ）・SMH（GXスタートデッキ各種）は複数の商品名に
  対応するとWeb検索で判明したが、`official-card-cache.json`を実際に検証したところ、
  商品間で番号を奪い合っておらず1つの連続した番号帯（SMA=1-59、SMH=1-131）として
  一貫していた（同一番号に異なるカード名が対応する例は0件）。フェーズ1の`SA`が
  「本当に番号が衝突する」ケースだったのに対し、こちらは「複数の商品名に跨る一つの
  カードプール」という別のパターンだと判明した。事前に全カードの(番号, 総数, 日本語名)
  を検証してから着手する運用が功を奏した。
- **【重要な発見2・要注意】商品内で印刷バッジが2種類に分かれるケースを発見**:
  `SMB`（プレミアムトレーナーボックス サン&ムーン、全18枚）は、公式サイト上では
  001-006番が`SMB`キー、007-018番が別キー`SM-XY`に分裂していた。原因は
  BW/XY調査時に「正体不明のため対象外」としていた裸の`XY`/`BW`キーと同種の現象だが、
  **今回は実際にカードの印刷バッジ自体が"SMB"と"XY"の2種類に分かれている**ことを
  画像で確認した（007-018番はN・フラダリ・プラターヌ博士等のXY時代キャラクターの
  再録カードで、"XY"という別マークが物理的に印字されている）。同様のパターンが
  `SMF`（013-020番の8枚）・`SMG`（013-041番の29枚）でも見つかり、いずれも裸の`XY`
  キー（396枚、多数の無関係な商品の再録カードが混在する共有プール）から該当分の
  cardIdを個別に`extraCardIds`で指定して補った（`XY`キーを丸ごとマージすると
  無関係なカードが大量混入するため、これは行わなかった）。
  - **既知の精度限界**: `codeAlias`はセット単位の1値しか持てないデータモデルのため、
    SMB/SMF/SMGはいずれも自セットのcodeAlias（"SMB"/"SMF"/"SMG"）を設定しているが、
    上記の裸`XY`由来のカード（SMB:12枚、SMF:8枚、SMG:29枚、計49枚）は実物の印字が
    セットのcodeAliasと一致しない（実際には"XY"と印字されている）。番号・カード名・
    レアリティ等の安全性に関わるデータには一切影響しないが、この49枚に限り
    タイトル生成の型番表示が実物と食い違う。対応する場合はcodeAliasをカード単位で
    持てるようデータモデルの拡張が必要（今回のタスクのスコープ外として記録のみ）。
- **想定外のレアリティコードが出たセット**: 無し（本弾フェーズで確認した6種＋
  今回追加のTR/CHR等でカバーできた。文字コード系はいずれもレアリティアイコンなし）。
- **受け入れ条件の確認結果**: `check-row-alignment.mjs`→疑いのあるセットなし、
  `detect-contaminated-en-names.py`→0件、`npx vitest run`→106件通過（既知の1ファイル
  問題を除く）、`git diff src/cardData.json`→追加のみ（3,466行挿入、削除0行）、
  14セット全552枚のファイル名をプログラムで突合→552/552一致、SMB/SMF/SMGの
  裸XY由来カードを含む画像を目視確認→実物の印字が"XY"であることを確認（上記の
  既知の精度限界の根拠）、`npm run build`成功。
- **フェーズ2完了。残る`SM0`/`SM-XY`単体扱い/`SMSMP`/`SMP1`/`SMP2`は今回のスコープ外**
  （ユーザー指示は「文字コード系SMA〜SMN」までのため）。うち`SM-XY`は上記の通り
  SMB/SMF/SMGへの統合で実質的に消化済み。

### 対応（2026-08-29）: SMB/SMF/SMGのcodeAlias精度限界の切り分け（ユーザー指示で調査）

上記「既知の精度限界」について、ユーザーから「(a)セット全体でXY印字なのか、
(b)セット内でXY印字とそれ以外が混在しているのか」を切り分けるよう指示があった。
SMB/SMF/SMGそれぞれの001番カード（自セットのcodeAliasキーに属する側）の実画像を
確認したところ、**3セットとも(b)：セット内で自セットのバッジ（SMB/SMF/SMG）と
"XY"バッジが混在**していることを確認した（SMB 001/018=ネストボール→"SMB"印字、
SMF 001/020=アクアパッチ→"SMF"印字、SMG 001/041=アクアパッチ→"SMG"印字。
いずれも007-018/013-020/013-041番の裸XY由来カードのみが"XY"印字）。

**現在のデータモデルでは対応不可**: `codeAlias`はセット（`c`）単位で1値しか持てない
フィールドのため、この(b)パターンに正しく対応するには「カード単位でcodeAliasを
上書きできる仕組み」（例: `k`配列の各カード行に5番目の要素としてcodeAliasを追加し、
無ければセット既定値にフォールバックする）が必要になる。これは`src/App.jsx`の
`displaySetCode`関数・`k`配列のスキーマ・関連するタイトル生成/CSV出力箇所すべてに
影響する設計変更であり、今回のカードデータ追加タスクのスコープを超えると判断し、
**実装せず報告のみに留めた（ユーザー指示通り）**。

**現状の挙動（対応するまでの暫定）**: SMB/SMF/SMG合計49枚（SMB:12枚, SMF:8枚,
SMG:29枚）は、タイトル生成・SKU等の型番表示が実物の印字（すべて"XY"）と食い違ったまま
になる。番号・カード名・レアリティ等の安全性に関わるデータには影響しないが、
この49枚を出品する際は型番表示を手動で"XY"に読み替えることを推奨する。
**次回対応する場合の設計案**: `k`配列の各行に`codeAlias`を持たせる（例:
`["007", "はかせのてがみ", "", "", "XY"]`）よう拡張し、`displaySetCode`相当のロジックを
セット単位からカード単位に変更する。

### 対応（2026-08-29）: SM0/SMP1/SMP2をフェーズ6へ再分類、SMSMPの重複回避メモ

未調査のまま残っていた`SM0`（4枚）・`SMP1`（13枚）・`SMP2`（25枚）は、依頼書の
フェーズ構成上「プロモ・その他」に該当する可能性が高いため、**フェーズ6（プロモ・
その他）の対象に含めることとし、フェーズ2のスコープからは外す**（ユーザー指示）。

- **未着手セット一覧（フェーズ6で扱う際の作業リスト）**: `SM0`（4枚、ja="ピカチュウと
  新しい仲間たち"、TCGdexに項目あり）・`SMP1`（13枚、cardId起点はニャビー関連プロモと
  推定、未検証）・`SMP2`（25枚、TCGdexに"名探偵ピカチュウ"として項目あり）。
  3セット合計42枚、いずれも番号体系・codeAlias・ja/yとも未検証のまま。
- **`SMSMP`（1枚）に関する重要なメモ（フェーズ6着手時に必読）**: `official-card-cache.json`
  上の`SMSMP`キー（「お茶会ごっこピカチュウ」1枚のみ）は、`details.php`で確認したところ
  実際の印刷バッジが**"SMP"**（フェーズ6で扱う予定の443枚のプロモ束と同一）だった。
  つまり`SMSMP`は独立した商品ではなく、`official-card-cache.json`のスキャン時に
  何らかの理由で`SMP`とは別キーに分類されてしまった1枚である可能性が高い。
  **フェーズ6で`SMP`（443枚）を取り込む際、`SMSMP`のこの1枚を重複して二重に
  取り込まないよう注意すること**（`SMP`のキャッシュ内にこのカードが既に含まれているか
  事前に確認し、含まれていなければ`SMP`側にこの1枚を統合する形で扱うのが妥当）。

### 対応（2026-08-29）: フェーズ3着手・MC取り込み中にpokemon-card.comからレート制限（HTTP 403）

フェーズ3（M世代）に着手し、`MC`（774枚、"MEGA Collection"。スタートデッキ100
バトルコレクション[2025-12-19発売]の母集団プール、フェーズ1の`SD`と同じ構造）を
事前に全774枚検証（総数742で統一・欠番0・重複0・レアリティアイコンなし・基本エネルギー
8種のみ無番号）した上で`TARGET_SETS`に追加し取り込みを開始したところ、
`details.php`が**HTTP 403（CloudFront側のWAFによるブロック、レスポンスヘッダに
`x-cache: Error from cloudfront`）を返すようになり中断した**。

- 単発リクエストで再確認したところ引き続き403が返ることを確認済み。単なる一時的な
  通信エラーではなく、本セッションでこれまでに行った大量のリクエスト（BW/XY・CP・
  文字マーク・SM本弾/文字コード系・MCの事前検証等、数千リクエスト規模）が
  レート制限の閾値を超えたことが原因と考えられる。
- **cardData.jsonへの書き込みは発生していない**（スクリプトの設計上、番号取得の
  失敗数が上限を超えた時点でファイル書き込み前に例外終了するため。`git status`で
  `src/cardData.json`に変更が無いことを確認済み）。データ破損・不整合のリスクは無い。
- `MC`のTARGET_SETSへの追加自体（`excludeCardIds`含む）は既に完了しているため、
  レート制限が解除された後に`node scripts/scrape-missing-sets.mjs`を再実行すれば
  そのまま再開できる。
- **フェーズ3はここで中断した。** 解除までの待機時間は不明（CloudFrontのレート制限
  ウィンドウは通常数分〜数時間程度）。再開する場合は、まず単発リクエストで
  ブロックが解除されているか確認してから`scrape-missing-sets.mjs`を実行すること。
  解除を確認せずに再試行を繰り返すと、ブロック期間が長引く可能性があるため避けること。

### 対応（2026-08-29）: フェーズ3完了（M世代 MEGAシリーズ欠落分）

レート制限解除を単発リクエストで確認した上で`DELAY_MS`を400→800へ引き上げ、
`CONCURRENCY=3`は維持してフェーズ3を再開・完了した。ユーザー指示で小さいセットから
処理する順序にしたところ、**当初の依頼書が「M世代」と想定していた13キーのうち
4キー（MDB・MPS08・MMB-P・MMB-S）が実際には2025年以降のMEGAシリーズとは
無関係の旧世代カードだった**ことが判明した（CPr/CPs/CPmと同種の「接頭辞の偶然の一致」
問題）。小さいセットから着手した結果、影響が最小限のうちに発見できた。

- **取り込んだセット一覧**（setCode / 枚数 / y / ja）:

  | setCode | 枚数 | y | ja |
  |---|---|---|---|
  | MA | 43 | 2025 | プレミアムトレーナーボックス MEGA |
  | MBD | 22 | 2025 | スターターセットMEGA「メガディアンシーex」 |
  | MBG | 22 | 2025 | スターターセットMEGA「メガゲンガーex」 |
  | M5 | 81 | 2026 | アビスアイ |
  | M4 | 120 | 2026 | ニンジャスピナー |
  | MP1 | 23 | 2025 | プロモカードパック第1弾 |
  | M2a | 250 | 2025 | MEGAドリームex |
  | MC | 766 | 2025 | スタートデッキ100 バトルコレクション |

  合計8セット・1,327枚。全セット画像で①codeAlias一致②発行年（©表記）が
  2025年以降であることの両方を確認してから採用した。

- **対象外にしたセット（4キー）— 旧世代カードと判明**:
  - **MDB（51枚）**: 001/046=ビクティニの実画像で**©2012 Pokémon**（BW期テンプレート、
    「特性」表記）を確認。2025年MEGAシリーズとは無関係
  - **MPS08（9枚）**: 001/009=氷空のシェイミの実画像がDP期特有の小サイズ・
    `Lv.62`表記（モダンMEGAシリーズにLv.表記は無い）。2007年前後のDPプロモと推定
  - **MMB-P/MMB-S（計116枚）**: 001/049=リザードンEXの実画像で**©2015 Pokémon**
    （XY期テンプレート、EX表記）を確認。「メガマスターデッキビルドBOX パワースタイル/
    スピードスタイル」という商品名自体は実在するが、この"M"は"Master"の意味で
    "MEGA"ではないと判明（2015年、XY・XY BREAK期の構築デッキ商品）
  - いずれも`official-card-cache.json`上のキー名（"M"接頭辞）から機械的に対象と
    判断していた場合に誤って取り込んでいたケース。CPr/CPs/CPmの教訓
  （「接頭辞が同じでも中身は別世代」）が今回も再現した。対応する場合は、これらは
    それぞれBW/DP/XY世代の対応タスクで扱うのが筋が良い（未着手のまま記録のみ）。
- **対象外にしたセット（1キー）— SA型の番号衝突**:
  - **MG（34枚）**: 事前検証で、001-013番のうち9番号（001,002,003,004,005,006,007,
    008,013）で異なるポケモン名が衝突していることを確認（例: 001が「ミュウツー」と
    「モンジャラ」の2通り）。010-012,014-016番は共通のトレーナー/エネルギーカードで
    名前が完全一致しdedupe可能だったが、Pokémon枠は2つの構築済みデッキが番号を
    共有する構造（フェーズ1の`SA`と同型）で一意特定不可能なため対象外とした。
- **一部のみ取り込んだセット — `M-P`（114枚）の内訳分離**:
  `M-P`キャッシュを検証したところ、実際には2種類が混在していた: 23枚は
  `MP1`バッジ（総数023で一貫、正常な単一商品）、残り91枚は`M-P`バッジのまま
  だが番号（"NNN/NNN"）自体が印字されていない個別プロモカードだった
  （既存のS-P/SV-Pと同種、位置マッチング方式の別スクリプトが必要な種類のプロモ
  ＝本スクリプトの対象外）。前者のみ`MP1`として取り込み、後者91枚は
  `excludeCardIds`で明示的に除外した（未着手のまま記録）。
- **新規レアリティコードを3種発見・追加**（`src/App.jsx`の`RARITIES`/
  `RARITY_EN_LABELS`/`HOLO_RARITIES`、`scripts/scrape-missing-sets.mjs`の
  `RARITY_CODE_MAP`）:
  - `ar`→**AR**（M4「ニンジャスピナー」084/083ハリマロン。既存の"AR"=Art Rareと
    一致、UI変更不要）
  - `sar`→**SAR**（同M4 115/083メガフラエッテex。既存の"SAR"=Special Art Rareと
    一致、UI変更不要）
  - `ma`→**MA**（M2a「MEGAドリームex」224/193メガユキメノコex。**RARITIES配列に
    存在しなかったため新規追加**。カード面に直接"MA"と印字されていることを画像で
    確認し、Web検索で「メガアタックレア」＝MEGAシリーズで新設された固有のレアリティ
    （技名が大きく英語でデザインされたアメコミ風イラスト、1BOX確定1枚）と確認した。
    ホロ加工も画像で確認できたため`HOLO_RARITIES`にも追加。**`RARITY_EN_LABELS`への
    英語表記追加は見送った**（ユーザー指摘、2026-08-29）: 日本語の呼称は複数の情報源で
    確認できたが、TRの"Trainer Rare"（mintvandal.com）のような英語圏収集コミュニティの
    確実な出典が見つからなかったため、直訳での追加はせず課題として記録するに留めた。
    未登録でも`RARITY_EN_LABELS[f.rarity] || ""`等の既存フォールバックで安全に動作する
    （表示が空欄になるだけ））
- **データ完全性のバグを発見・修正（重要）**: `M2a`の初回取り込み時、cardId 49982
  （222/193「ジャミングタワー」、SR）が一時的な通信エラーで取得失敗した。失敗数が
  `MAX_FAILED_CARDS`(5)未満だったため許容され、**総数(193)を超える「secret」範囲の
  番号だったために1〜totalの欠番チェックをすり抜け、書き込み自体は「成功」として
  完了してしまった**（実際には222番が丸ごと欠落した状態でcardData.jsonに書き込まれて
  いた）。目視確認の過程で発覚し、該当セットをcardData.jsonから削除した上で
  `extraCardIds`で再取得して補完した。**この種の欠落は「total以下の欠番チェック」
  だけでは原理的に検出できない**ため、念のためフェーズ1〜3で今回追加した全セットに
  ついて「1〜セット内最大番号」の欠番を機械的に再チェックしたが、他に同種の欠落は
  無かった（M2aのみ）。
  **恒久対応（ユーザー指摘、2026-08-29）**: 当初は該当の1枚を追加しただけで、
  検証ロジック自体は直していなかった（フェーズ4以降で同じ種類の欠落が再発しうる
  状態だった）。指摘を受け、`scripts/scrape-missing-sets.mjs`の欠番チェックを
  「1〜total」から**「1〜実際に見つかった最大番号」**に拡張した。シークレット
  カードの番号も連番であることが期待できるため、total超過範囲の欠落もこの拡張で
  検出できるようになった（例: 743,744,745...と続くべきところで744が欠ければ検出）。
  **さらにユーザー指摘（2026-08-29、2回目）を受け、「修正した」という報告だけでは
  不十分と判断し、以下を追加で実施した**:
  - 検証ロジック（重複・欠番・レアリティ未知チェック一式）を`main()`から
    `validateAndBuildK(details, code)`という独立した関数に切り出し、`export`した。
  - `scripts/scrape-missing-sets.test.mjs`を新規作成し、**M2aの事故を模したデータ
    （total=193に対し194〜250のシークレット範囲から222番だけを意図的に抜いた配列）を
    `validateAndBuildK`に渡すと`欠番があります`エラーがthrowされることをテストで確認**
    （7件のテストケース、うち1件がこのM2a再現テスト）。`npx vitest run`で実際に
    グリーンになることを確認済み（修正前のロジックに戻すと該当テストがREDになることも
    手元で確認した）。
  - モジュールを直接importしただけで`main()`（実際のスクレイピング）が走ってしまわない
    よう、`check-row-alignment.mjs`と同じ`pathToFileURL`ガードをCLIエントリポイントに
    追加した。
  - **`scripts/scrape-bw-xy.mjs`（BW/XY/CP/XYマークの取り込みで使用、以後のフェーズでは
    使わない旧スクリプト）にも同じロジック修正を移植した。** 以後のフェーズでは
    `scrape-missing-sets.mjs`のみを使うため実害は無かったはずだが、万一再実行された
    場合に備えて同期した（ただしこちらは`validateAndBuildK`への切り出し・テストは
    行っていない。使われない前提のスクリプトへの投資を最小限にするため）。
- **受け入れ条件の確認結果**: `check-row-alignment.mjs`→疑いのあるセットなし、
  `detect-contaminated-en-names.py`→0件、`npx vitest run`→106件通過（既知の1
  ファイル問題を除く）、`git diff src/cardData.json`→追加のみ（削除0行）、
  8セット全1,327枚のファイル名をプログラムで突合→1,327/1,327一致、8セットから
  画像を目視確認（MC は001/742・401/742・766/742＝ゴールド秘密レアの3枚を含む
  計10枚）→全件一致、`npm run build`成功。

### 未着手・要再分類リスト（全フェーズ完了後にまとめて対応）

「未収録カード全取得」タスクの過程で、当初の想定と異なる世代・構造と判明し
今回は着手しなかったセットの一覧。個別の発見経緯は各フェーズの節を参照。
**ユーザー指示により、全フェーズ完了後にまとめて扱う（現時点では着手しない）。**

| setCode | 枚数 | 発見時期 | 実体 | 対応方針（未着手） |
|---|---|---|---|---|
| MDB | 51 | フェーズ3 | 2012年BW期のカード（©表記で確認） | BW世代の対応タスクで扱う |
| MPS08 | 9 | フェーズ3 | 2007年前後のDP期プロモ（Lv.表記・小サイズ画像） | DP世代の対応タスクで扱う |
| MMB-P | 58 | フェーズ3 | 2015年XY期「メガマスターデッキビルドBOX パワースタイル」（©表記で確認） | XY世代の対応タスクで扱う |
| MMB-S | 58 | フェーズ3 | 同上「スピードスタイル」 | XY世代の対応タスクで扱う |
| MG | 34 | フェーズ3 | SA型の番号衝突（2構築デッキが番号帯を共有、一意特定不可） | カード単位のサブセット識別の仕組みが必要（設計課題） |
| M-P（残り） | 91 | フェーズ3 | 無番号の個別プロモカード（S-P/SV-Pと同種） | 位置マッチング方式の別スクリプトが必要（本スクリプトの対象外） |
| SM0 | 4 | フェーズ2 | 未調査 | フェーズ6（プロモ・その他）で扱う |
| SMP1 | 13 | フェーズ2 | 未調査 | フェーズ6（プロモ・その他）で扱う |
| SMP2 | 25 | フェーズ2 | 未調査 | フェーズ6（プロモ・その他）で扱う |
| SA | 122 | フェーズ1 | SA型の番号衝突（5構築デッキが番号帯を共有） | カード単位のサブセット識別の仕組みが必要（設計課題） |
| LP | 92 | フェーズ5 | 無番号の個別プロモカード（S-P/SV-P/M-P残りと同種） | 位置マッチング方式の別スクリプトが必要（本スクリプトの対象外） |
| XYP | 456 | フェーズ6-2 | 無番号の個別プロモカード。XY-Pと実バッジが重複している可能性あり | 位置マッチング方式の別スクリプトが必要。XY-Pとの重複関係を先に確認 |
| SMP | 443 | フェーズ6-2 | 無番号の個別プロモカード（SMSMPの1枚を含む可能性） | 位置マッチング方式の別スクリプトが必要。SMSMP重複に注意 |
| BWP | 246 | フェーズ6-2 | 無番号の個別プロモカード | 位置マッチング方式の別スクリプトが必要 |
| DPP | 175 | フェーズ6-2 | 無番号の個別プロモカード。一部が実バッジ"MPS"（MPS08系統）の可能性 | 位置マッチング方式の別スクリプトが必要。実バッジ単位の仕分けが必要 |
| XY-P | 14 | フェーズ6-2 | 無番号。実バッジは"XYP"（ハイフン無し）＝上記XYPと同一の可能性 | XYPとの重複関係を先に確認してから対応方針を決める |
| DPtP | 65 | フェーズ6-2 | 無番号の個別プロモカード | 位置マッチング方式の別スクリプトが必要 |
| ENE | 85 | フェーズ6-3 | 基本エネルギーのみ・全件無番号 | 位置マッチング方式の別スクリプトが必要（本スクリプトの対象外） |

合計18セットコード・2,041枚が保留中。うち`MG`/`SA`は同一の設計課題（番号帯を共有する
複数商品を1つのcardData.jsonエントリで表現できない）のため、対応する場合はまとめて
設計すること。無番号プロモ群（LP/XYP/SMP/BWP/DPP/XY-P/DPtP/ENE/M-P残り91枚、合計1,667枚）は
S-P/SV-Pと同じ位置マッチング方式のスクリプトが必要なため、対応する場合はまとめて設計すること。

**フェーズ1〜6完了時点でのcardData.json未収録・保留の全体像**: 上記の保留セット18コード
以外に、`official-card-cache.json`（322キー）にはDP/DPt世代の本弾（`DP1`〜`DP5`,
`DPt1`〜`DPt4`等、対象外方針は継続）や、素性不明の英数字コード（`BGSt`, `BKB`, `Bb`等
約90キー）が未調査のまま残っている。これらはフェーズ1〜6の依頼書スコープに含まれて
いなかったため今回は着手していない。対応する場合は個別の調査フェーズが必要。

### 対応（2026-08-30）: フェーズ5完了（LEGEND世代）＋ LEGENDカードのdetails.php解析バグを発見・修正

`L1-Bhg`(ハートゴールドコレクション) `L1-Bss`(ソウルシルバーコレクション) `L2-B`
(よみがえる伝説) `L3-B`(頂上大激突) `LL`(強化パック ロストリンク) `L2-Sb` `L2-Sh`の
7セット・378枚を追加した（`LP`はLPは番号なしの個別プロモの束と判明したためフェーズ6送り）。

- **番号形式の確認**: 1セット目（L1-Bhg）でBW/XYと同じNNN/NNN形式であることを確認。
  route Aがそのまま使えると判断して着手した。
- **【最重要】LEGENDカード（2枚1組で1枚として機能する特殊カード。例:
  「エンテイ&ライコウLEGEND」)は、official-card-cache.jsonのスキャンから種別ごと
  丸ごと漏れていた**（BREAKカードの数枚単位の漏れとは規模が異なり、jaNameに"LEGEND"を
  含むカードが322セット中どこにも1件も無いことを確認した）。周辺cardIdを1件ずつ
  探索して発見し、`extraCardIds`で個別に補った。
- **さらに、details.phpの1ページに(上)(下)両方の番号・レアリティが埋め込まれている
  ことを発見した**（例: cardId 25693のページに"063/080(上)"と"064/080(下)"が
  連続して出現）。既存の`fetchCardDetail`は`.match()`（最初の1件のみ取得）を
  使っていたため、この構造だと下半分の番号を完全に見落とす作りになっていた。
  加えて、下半分側の表記には`"080 &nbsp;"`のように総数の直後に半角スペースが入る
  表記ゆれがあり、既存の正規表現（スペース不許容）ではそもそもマッチしなかった。
  **`.matchAll()`で全件抽出し、番号とレアリティを件数分ペアで返すよう
  `fetchCardDetail`を書き換え、正規表現にも`\s*`を追加した**（`parseCardDetailsFromHtml`
  としてHTML解析部分を独立関数に切り出し、`scrape-missing-sets.test.mjs`に単体テストを
  追加。通常カード1件・LEGENDカード2件・スペース表記ゆれ・番号無しカードの4パターンを
  カバー）。この修正により、当初「details.phpからも周辺探索からも下半分が一切見つからない」
  ように見えていた事象は、実際には**同一ページ内に両方の情報が存在するのに解析漏れ
  していただけ**と判明し、外部情報源（Bulbapedia等）に頼らずdetails.php単独で
  上下とも検証済みの状態で取り込むことができた。
  - `extraCardIds`（LEGENDカードの上半分のcardId。1件で上下2件分のデータが返る）:
    L1-Bhg=[25027]、L1-Bss=[25186]、L2-B=[25693,25697,25701]、
    L3-B=[26170,26171,26172]、LL=[26067]
  - 画像は1ページに1枚しか無く、上下2枚の物理カードを1枚の合成画像（上下を
    縦に並べたもの）で表現していることを画像目視で確認した。ソース画像が
    共有されるため、上下それぞれのファイル名に同じ画像を複製保存している
    （番号ごとに1ファイルという既存の命名規則を優先し、画像を分割クロップする
    処理は追加していない）。
- **新規レアリティコードを4種発見・追加**（LEGEND世代は`ic_rare_{code}.gif`の
  `{code}`がそれ以前の世代と異なり1文字の命名規則だった）:
  - `c`→**C**、`u`→**U**、`r`→**R**（いずれも既存表記と一致、追加のUI変更は不要）
  - `s`→**RH**（Rare Holo。「r」と同じ★マークだがホロ箔押し加工がある。1999年の
    英語版TCG発売当初から使われている公式レアリティ名。L1-Bhgの003/070スピアー=r
    （非ホロ星）と025/070オーダイル=s（ホロ星）を画像で比較し、模様の有無を確認した
    上で新規追加）
  - `ss`→**LEGEND**（LEGENDカード専用。英語版TCGがこの世代のシリーズ名・レアリティ名
    として"LEGEND"をそのまま公式に使っているため直訳ではなくそのまま採用した）
  - いずれも`RARITIES`/`RARITY_EN_LABELS`/`HOLO_RARITIES`（src/App.jsx）に追加。
  - `SERIE_EN_NAMES`にも`L`→`"HeartGold & SoulSilver"`を追加した（国際版TCGでも
    この世代はゲーム本編と同じHGSSブランディングで発売されているため）。
- **`L2-Sb`/`L2-Sh`（各19枚）**: 構築済みデッキ商品と推定されるが、TCGdex・Web検索
  とも商品名を特定できなかったため`ja`は空欄のまま。番号衝突（SA/MG型）が無いことは
  事前検証済み（両セットとも重複・欠番なし）。
- **`LP`（92枚）**: 全件`details.php`で番号（"NNN/NNN"表記）自体が印字されていない
  ことを確認。M-P残り91枚・S-P・SV-Pと同じ、位置マッチング方式が必要な個別プロモの束
  と判明したため、フェーズ6（プロモ・その他）に送った。上記「未着手・要再分類リスト」
  に追記。
- **受け入れ条件の確認結果**: `check-row-alignment.mjs`→疑いのあるセットなし、
  `detect-contaminated-en-names.py`→0件、`npx vitest run`→117件通過
  （既知の1ファイル問題を除く。うち11件が今回追加したscrape-missing-sets.test.mjs、
  4件がparseCardDetailsFromHtmlの新規テスト）、`git diff src/cardData.json`→
  追加のみ（削除0行）、7セット全378枚のファイル名をプログラムで突合→378/378一致、
  全セットについて「1〜最大番号」の欠番再チェック→0件、3セットから画像を目視確認
  →全件一致、`npm run build`成功。

### 対応（2026-08-30）: フェーズ6-1完了（SV系未収録セット）

`SVI` `SVM` `SVG` `SVN` `SVJL` `SVJP` `SVOD` `SVOM`の8セット・417枚を追加した。
着手前に「M2aのシークレット範囲欠番チェックが仕組みとして機能しているか」を
改めて確認した上で着手した（`validateAndBuildK`のテストが green であることを再確認）。

- **取り込んだセット一覧**（setCode / 枚数 / y / ja）:

  | setCode | 枚数 | y | ja |
  |---|---|---|---|
  | SVI | 66 | 2024 | バトルアカデミー |
  | SVM | 175 | 2024 | スタートデッキGenerations |
  | SVG | 52 | 2023 | スペシャルデッキセットex フシギバナ・リザードン・カメックス |
  | SVN | 45 | 2025 | デッキビルドBOX バトルパートナーズ |
  | SVJL | 21 | 2024 | バトルマスターデッキ テラスタル「リザードンex」 |
  | SVJP | 19 | 2024 | バトルマスターデッキ「パオジアンex」 |
  | SVOD | 19 | 2025 | スターターセットex「ダイゴのダンバル&メタグロスex」 |
  | SVOM | 20 | 2025 | スターターセットex「マリィのモルペコ&オーロンゲex」 |

  事前検証で全8セットとも番号衝突（SA/MG型）・欠番（total超過範囲を含む）とも無いことを
  確認してから着手。SVIのみ基本エネルギー8種が無番号のため`excludeCardIds`で除外
  （既知パターン）。それ以外は一発で番号検証OK。
- **codeAlias**: 全8セット、details.phpのimg-regulation altテキストで確認（全件一致）。
  SVI・SVM・SVODの3セットは実カード画像でも照合し、©2023〜2025表記・"svI"/"svM"/"svOD"
  バッジを確認済み。
- **ja/y の出典**: TCGdexにこれらのセット名が無かったため、Web検索（トレ研ポケカの
  エキスパンションマーク一覧・各商品の公式発表記事）に基づく（確度: 高。複数の情報源で
  発売日が一致することを確認済み）。SVMは「スタートデッキGenerations」（9地方テーマの
  構築済みデッキを束ねた母集団プール、175枚。フェーズ1のSD・フェーズ3のMCと同じ構造）。
- **想定外のレアリティコードが出たセット**: 無し（全8セットともレアリティアイコン無し、
  構築済みデッキ/スターター商品の既知パターン）。
- **受け入れ条件の確認結果**: `check-row-alignment.mjs`→疑いのあるセットなし、
  `detect-contaminated-en-names.py`→0件、`npx vitest run`→117件通過（既知の1
  ファイル問題を除く）、`git diff src/cardData.json`→追加のみ（削除0行）、
  8セット全417枚のファイル名をプログラムで突合→417/417一致、全セットについて
  「1〜最大番号」の欠番再チェック→0件、3セットから画像を目視確認→全件一致、
  `npm run build`成功。
- **フェーズ6-1完了。フェーズ6-2（世代別プロモ）は未着手。**

### 対応（2026-08-30）: フェーズ6-2（世代別プロモ）— 全7セットが無番号と確認、取り込み対象なし

`XYP`(456) `SMP`(443) `BWP`(246) `DPP`(175) `XY-P`(14) `DPtP`(65)、および
フェーズ5で確認済みの`LP`(92)を対象に、番号形式を調査した。

- **全7セットとも、details.phpで"NNN/NNN"表記自体が印字されていないことを確認した**
  （各セット14〜17枚を均等サンプリングし、全サンプルでnumFound=0）。既存の`LP`・
  `M-P`残り91枚・S-P・SV-Pと同じ、位置マッチング方式の別スクリプトが必要な個別プロモの
  束であり、本スクリプト（NNN/NNN形式前提）の対象外と判断した。無理に取り込まず、
  下記「未着手・要再分類リスト」に記録するのみとする（ユーザー指示通り）。
- **副次的な発見（未着手のまま記録）**:
  - `XY-P`（14枚）はimg-regulation altテキストが実際には`"XYP"`（ハイフン無し、
    456枚の大きい方のセットと同一）だった。内部cacheキーが枝分かれしているだけで、
    実物のバッジは大きい方の`XYP`と同じ可能性がある（`SMSMP`が実は`SMP`の一部
    だったのと同型のパターン）。対応する場合はこの重複関係を先に確認すること。
  - `DPP`のサンプル中に1件、altテキストが`"MPS"`（フェーズ3で対象外にした`MPS08`と
    同系列と思われる、DP期のナンバリング付きプロモシリーズ）のカードが混在していた。
    `DPP`という1つのcacheキーの中に複数の異なる実バッジが混在している可能性があり、
    対応する場合は`SMB`/`SMF`/`SMG`で行ったのと同様、実バッジ単位での仕分けが必要になる
    見込み（今回は追加調査せず記録のみ）。
- **フェーズ6-2完了（取り込み対象0件）。フェーズ6-3（記念・特殊）は未着手。**

### 対応（2026-08-30）: フェーズ6-3完了（記念・特殊セット）

`WCP` `20th` `SM0` `SMP1` `SMP2` `WCS23`の6セット・257枚を追加した。
`ENE`(85枚)は全件基本エネルギーのみ・無番号と確認したため対象外（未着手リストに記録）。

- **取り込んだセット一覧**（setCode / 枚数 / y / ja）:

  | setCode | 枚数 | y | ja |
  |---|---|---|---|
  | WCP | 108 | 2007 | ワールドチャンピオンズパック |
  | 20th | 77 | 2016 | ポケットモンスターカードゲーム スターターパック |
  | SM0 | 4 | 2016 | ピカチュウと新しい仲間たち |
  | SMP1 | 13 | 2017 | 月刊コロコロコミック2017年1月号付録「イワンコ全力デッキ」 |
  | SMP2 | 25 | 2019 | 名探偵ピカチュウ |
  | WCS23 | 30 | 2023 | ポケモンワールドチャンピオンシップス2023横浜 記念デッキ「ピカチュウ」 |

  事前検証で6セットとも番号衝突・欠番なし。20thのみ基本エネルギー7種が無番号のため
  `excludeCardIds`で除外。全セット画像で内容・年代を確認してから採用した。
- **【重要】WCPは2007年発売と判明（DP世代該当）**。フェーズ3のMDB/MPS08/MMB-P/MMB-Sと
  同種の「接頭辞が別世代と偶然一致」ケースだが、001/108という独立した連番を持ち
  DPBP型の番号衝突問題が無いことを確認できたため、CPr/CPs/CPmと同じ判断で取り込んだ
  （画像で©2007表記を確認済み）。
- **【重要】`20th`は既存の`CP6`（拡張パック20th Anniversary、フェーズ4で取り込み済み、
  99枚）とは別物と判明**。両者ともテーマは「20周年」だが、cardIdの範囲が完全に
  重複しないこと（WCP: 31651-31734、CP6: 32207-35872）を確認し、実体は
  「ポケットモンスターカードゲーム スターターパック」（1996年第1弾スターターパックの
  20周年復刻版、2016年発売）という別商品と判明した。CP6の重複取り込みでは無い。
- **`ENE`（85枚）**: 全85枚が9種類の基本エネルギー名義で、details.phpで確認したところ
  1枚も番号が印字されていなかった。ユーザーの事前予想通り。位置マッチング方式が
  必要な無番号プロモとして「未着手・要再分類リスト」に追記した。
- **想定外のレアリティコードが出たセット**: 無し。
- **受け入れ条件の確認結果**: `check-row-alignment.mjs`→疑いのあるセットなし、
  `detect-contaminated-en-names.py`→0件、`npx vitest run`→117件通過（既知の1
  ファイル問題を除く）、`git diff src/cardData.json`→追加のみ（削除0行）、
  6セット全257枚のファイル名をプログラムで突合→257/257一致、全セットについて
  「1〜最大番号」の欠番再チェック→0件、6セット全件を画像で目視確認→全件一致、
  `npm run build`成功。
- **フェーズ6完了**（6-1: SV系未収録、6-2: 世代別プロモ、6-3: 記念・特殊）。

### 対応（2026-08-30〜31）: 既存セットのシークレット範囲欠落の調査・パッチ（新規セット追加とは別種のタスク）

フェーズ6完了後、`cardData.json`の枚数が`official-card-cache.json`より少ないセットが
**56セット・約1,689枚**見つかった。原因は元のTCGdexベースのデータ生成が「セットの公式総数
（`of`）まで」で止まっており、総数を超える番号帯にあるSR/SAR/UR/HR等の高額シークレットカードが
系統的に欠落していたこと。**新規セットの追加（CREATE）ではなく、既存セットへの追記（PATCH、
`of`は変更せず`k`配列に追加するのみ）**という、これまでと異なる種類の作業。

- **`scripts/patch-existing-sets.mjs`を新規作成**: `scrape-missing-sets.mjs`の
  `validateAndBuildK`/`parseCardDetailsFromHtml`/`fetchCardDetail`等をそのままimportして
  再利用（ロジックの複製をしていない）。既存セットが見つからない場合はエラーで停止する
  （新規セット作成は行わない設計）。既存番号との重複時は、名前が完全一致すればスキップ、
  末尾の括弧注記だけが違う場合（`stripParenSuffix()`、`scrape-official-images.mjs`の
  `matchName()`と同じ既知パターン）もスキップ、それ以外の食い違いは例外を投げて停止する。
  1セットごとに`cardData.json`へ逐次書き込み（クラッシュセーフ）。
- **【重要】整合性チェック手法を「内部の名前重複パターン」から「official-card-cache.json /
  details.phpとの直接照合」に変更した**。ユーザー指摘により、全254セット中162セットで
  「同一セット内の名前重複」（通常番号カードとAR/SR版が同名になる正常な構造）があることが
  判明し、内部パターンでは破損を検出できないと分かったため。56セット全件について、
  各セットの先頭・中間・末尾からサンプリングしたカードの(番号, 日本語名)を
  `official-card-cache.json`のcardId経由で`details.php`から再取得し、`cardData.json`の
  内容と直接突き合わせる事前チェックスクリプト（`verify-56sets.mjs`、scratchpad内、
  リポジトリには含めていない）を作成・実行した。
- **【最重要の発見】S4a等30セットで、既存データそのものが破損していると判明**。S4aは
  190枚全件がポケモンカードで、トレーナーズ・エネルギーが1枚も含まれていなかった
  （通常あるはずの構成が欠けている）。cardData側の番号とキャッシュ側の実際の番号を照合した
  結果、**S4aの190枚すべてで番号が誤っていた**（トレーナーズが除外され、ポケモンだけが
  詰めて番号を振り直された状態と判明）。同様の「トレーナーズカードが1枚も無い」セットが
  他に29セットあることも、日本語名のキーワード検出（ボール/ネット/博士/エネルギー/
  スイッチ等を含むカードが0件）で機械的に発見した: S8b, S8a, S10a, S11a, SI, SVAL, SVAM,
  SVAW, SVC, SVEL, SVEM, SH, SV5M, S10D, S10P, S10b, S11, S4, S5a, S6a, S6H, S6K, S7D, S7R,
  S8, SV6, SV6a, S5I, S5R（S4aと合わせて計30セット）。ユーザー指示により、**この30セットは
  今回のシークレット追加バッチから完全に除外し、別タスクとして保留**した
  （`patch-existing-sets.mjs`のパッチ対象には一切含めていない。破損の修復方法・範囲の特定は
  未着手）。
- **破損の無い26セットのみでシークレット追加を実施**。優先度順（S12a, SV5K, S9a, SV4a,
  SVI, SV10, SV8, SVHK, SVHM, CP4, XYA, XYB, XYC, XYD, XYE, XYH, SM1p, SM2p, SMA, SMD,
  SML, MC, L2-Sb, L2-Sh, 20th, WCS23）で1セットずつ検証・パッチした。実際に新規カードが
  追加できたのは6セットのみ（他20セットは事前検証時点で既にキャッシュと完全一致しており
  追加対象なし。以前のフェーズで既に取得済みだったか、そもそもシークレットが存在しない
  セットだった）:

  | setCode | 追加枚数 | 追加後の`k`件数 |
  |---|---|---|
  | SV5K | +51 | 100 |
  | SV4a | +40 | 360 |
  | SV10 | +34 | 132 |
  | SV8 | +32 | 138 |
  | SVHK | +31 | 55 |
  | SVHM | +31 | 55 |

  合計219枚。`of`（総数）フィールドは全セットとも無変更（`git show HEAD:src/cardData.json`
  との内容レベル比較で、削除0件・`of`変更0件・既存カード改変0件・新規セット0件を
  プログラムで確認済み）。SVHK/SVHMは過去に行ズレで全体空欄化した経緯があるセットだったが、
  cardData側の枚数(55)が`of`(53)と一致しないのは今回追加したシークレット2枚分
  （通常53枚+シークレット2枚）が原因であり、行ズレの再発ではないことを確認した。
- **新規レアリティコード発見**: `csr`(CSR)・`hr`(HR)。いずれも`src/App.jsx`の`RARITIES`
  配列には既存フェーズで追加済みだったため、`scripts/scrape-missing-sets.mjs`の
  `RARITY_CODE_MAP`にマッピングを追加しただけで対応できた（UI側の変更は不要）。
- **`excludeCardIds`の再利用**: SVI・CP4・SMA・SML・MC・20thは新規セット作成時
  （フェーズ1〜6）に判明していた無番号の基本エネルギーcardIdをそのまま
  `patch-existing-sets.mjs`側のTARGET_PATCHESにも設定する必要があった（既存の
  `scrape-missing-sets.mjs`側の設定は自動的には引き継がれない、別ファイルのため）。
- **画像確認手法の注意点（今回の検証で判明）**: 新規追加したシークレットカードの
  ファイル名の`／{total}`部分は、既存カードの生成当時のtotal（旧・より小さい値）とは
  桁数が異なる場合がある（例: SV10は既存カードが`／98`＝2桁パディングで生成済みだが、
  シークレット追加で最大番号が132になった）。**これはバグではなく想定通りの挙動**:
  `build-image-index.mjs`のインデックスキーは`extractLocalId`でファイル名から抽出した
  localIdを`parseInt`した数値（パディング桁数に依存しない）を使うため、新旧のパディングが
  混在していても画像は正しく引ける。新規追加219枚全件について、この方式で
  `imageIndex.json`上に対応するキーが存在することを確認済み（`node
  scripts/build-image-index.mjs`再実行後、16,681件・jpg=14,498/png=2,183）。
- **受け入れ条件の確認結果**: `check-row-alignment.mjs`→疑いのあるセットなし、
  `detect-contaminated-en-names.py`→0件、`npx vitest run`→117件通過（既知の1
  ファイル問題を除く）、`git show HEAD:src/cardData.json`との内容レベル比較→
  削除0・`of`変更0・既存カード改変0・新規セット0・追加219枚（内訳は上表と一致）、
  新規追加219枚全件について`imageIndex.json`上の画像存在をプログラムで確認→
  219/219一致、`npm run build`成功。
- **未解決・保留（ユーザーへの報告・指示待ち）**:
  1. **S4a等30セットのデータ破損**: 修復方針（全件作り直し vs 該当箇所のみ修正）が
     セットごとに異なる可能性があり、個別調査と修復方法の決定が必要。今回は一切
     手を付けていない。→ **2026-08-31、うち10セットに着手。詳細は下記参照**
  2. **S12aの251-258番（基本エネルギーのシークレット8枚）**: Web/リテール検索で存在は
     確認できたが、`official-card-cache.json`のスキャン・cardId近傍探索のいずれからも
     見つけられず、未回収のまま。
  3. **VS1/web1のpcg-search.com画像取得URL**: 既知の`/img/vs/vs0{NNNN}.png`パターンで
     既知の正しい番号ですら404になる状態を確認。サイト構造が変わった可能性があり、
     未調査。
  4. **未着手・要再分類リスト**（無番号プロモ群: XYP/SMP/BWP/DPP/XY-P/DPtP/LP/M-P残り91/
     ENE、番号衝突セット: SA/MG）は引き続き未着手。

### 対応（2026-08-31）: スキップセット（英語版・対象外）22セット3,282枚を削除

CLAUDE.md記載の「スキップセット（英語版 / 対象外）」（`SC*`10セット、`SV*s`7セット、`SVDs`、
`SDL`/`SDM`/`SDP`、`CSMPiC`、計22セット・3,282枚）を`cardData.json`から削除した。
削除前に全22セットで日本語名0件（`CSMPiC`の21件を除く）・画像0件（imageIndex上のエントリ
なし、`CSMPiC`含め22セット全件）であることを確認し、`src/App.jsx`にもこれらのsetCodeへの
参照が無いことを確認済み。日本語版ではなく検索結果に空タイルとして並ぶだけの状態だった
（ユーザー指摘通り）。内容レベル比較（`git show HEAD:src/cardData.json`との比較）で、
削除された22セット以外に変更が無いことを確認済み。

### 対応（2026-08-31）: S4a型データ破損の10候補セットを調査・9セットを全件取り直し

前回報告した「S4a等30セットのデータ破損」のうち、cardDataの枚数がキャッシュより大幅に
少なく、かつトレーナーズカードが0件という条件に一致した10セット（S4a, SI, S8b, S11a,
SV6a, S10D, S5a, S6H, S7D, SM2p）について、ユーザー指示に基づき個別調査・修復した。

- **調査方法**: 先頭・中間・末尾からのサンプリング照合（9件/セット）→ 10セット全件が
  「疑いあり」と判定されたため、次に全セットについて official-card-cache.json 経由で
  cardIdを全件列挙し、details.phpから(番号, 総数, レアリティ, 日本語名)を1件ずつ取得する
  **全数照合**を実施した（サンプリングでは判定を誤る実例が複数出たため。後述）。
- **【重要な発見】サンプリングだけでは破損の実態を見誤る**: 当初9セット全てが
  「先頭〜末尾のどこかで不一致」という同じ"SUSPECT"判定だったが、全数照合の結果、
  実際の破損パターンは大きく2種類に分かれることが判明した。
  1. **頭部は正常・末尾（トレーナーズ/エネルギー区間）のみ破損**: S4a, S8b, S11a, SV6a,
     S10D, S5a, S6H, S7Dの8セット。番号の若い方（ポケモンカード）は完全に正しく、
     末尾のトレーナーズ/エネルギー区間だけがポケモン名の重複データで埋められていた
     （原因は元のTCGdexベースのデータ生成が、この区間のカードだけ取得できずに
     前方のポケモン名を使い回して穴埋めした一種の不具合と推測される）。
  2. **ほぼ全件が破損**: SI（422件中409件不一致）。番号6番目から既に食い違っており、
     部分修正では対応できないレベル。
  3. **破損なし**: SM2p。サンプリング調査で唯一"OK"判定だったが、全数照合でも
     0件不一致を確認（curLen=57がof=49を上回っており、既にシークレット込みで
     完全なデータだった）。**この10セットのうちSM2pだけは対象外**とした。
  - なお「頭部は正常」の8セットも、レアリティ（`k[3]`）フィールドは頭部を含め
    ほぼ全件が空文字列のままだった（真の値はC/U/R/RR/SR等）。元のTCGdex由来データが
    そもそもレアリティを持っていなかったためで、末尾の名前破損とは別の問題だが、
    truthデータで頭部も含めて丸ごと置き換えることで同時に解消した。
- **【重要】部分修正ではなく全件置き換えを採用**: ユーザー方針（「番号が全件ズレている
  セットは丸ごと取り直す、部分的に正しいデータは危険」）に対し、8セットは「一部だけ
  食い違う」ケースだったが、全数照合により1〜最大番号の**全件が検証済みtruthとして
  手元にある**状態になったため、「正しい部分と壊れた部分の境界を推測して部分修正する」
  よりも「セット全体をtruthで置き換える」方が安全かつ単純（境界の見誤りリスクが無く、
  頭部のレアリティ欠落も同時に解消する）と判断し、9セット全件（SI含む）を統一的に
  「truthで丸ごと置き換え」する方針にした。`of`（総数）は変更していない
  （truth側の`total`が全セットで既存の`of`と完全一致することを事前に確認済み）。
- **`scripts/rebuild-corrupted-sets.mjs`を新規作成**: `scrape-missing-sets.mjs`の
  `validateAndBuildK`/`fetchCardDetail`/`downloadImage`をそのまま再利用。事前に取得・
  保存済みのtruthデータ（scratchpad内、リポジトリ非包含）を読み込み、該当セットの`k`配列を
  丸ごと置き換える。画像も新しいk配列に基づき再ダウンロードし、**旧ファイル名（変更前の
  日本語名・レアリティで生成されていたもの）のうち新ファイル名一覧に含まれないものは
  「孤児画像」として削除**した（名前・レアリティが変わって不要になった旧画像が
  `imageIndex.json`に紛れ込み、誤った画像が表示されるリスクを防ぐため）。1セットごとに
  逐次書き込み（クラッシュセーフ）。
- **SI特有の問題: 非数値localId（"DAR"/"FIG"/"FIR"/"GRA"/"LIG"/"MET"/"PSY"/"WAT"の
  8件）を発見・除去**: 旧cardDataのSIには、通常の3桁数値ローカルIDとは別に、アルファベット
  コードの8件（推定: 特殊エネルギーの属性コード）が存在した。カード名が「ジガルデ」
  「バクガメス」等の**ポケモン名**になっており、特殊エネルギーの名前としては明らかに
  不自然。official-card-cache.jsonの563件を全数照合した範囲にはこの8件に対応する
  カードが一切見つからず（＝サイト側の番号体系のどこにも存在しない）、正しい内容を
  検証できなかったため、truthに基づく置き換えでは**この8件を復元せず除外**した
  （不明な内容を推測で埋めない方針を優先）。SIの新k配列は414(of) + 5(シークレット) =
  419件で、非数値IDは含まれない。この8件の正体（本当に存在するカードなのか、
  当初から誤混入したデータなのか）は未解明のまま。
- **受け入れ条件の確認結果**:
  - サンプリング→全数照合→置き換えの全段階で使ったスクリプトはscratchpad内のみで
    実行し、リポジトリには`scripts/rebuild-corrupted-sets.mjs`のみ追加した
  - 置き換え結果（新k配列）とtruthデータを1件ずつ突合（番号・日本語名・レアリティ）→
    9セット合計1,544件で不一致0件、重複0件、欠番0件
  - `git show HEAD:src/cardData.json`との内容レベル比較→スキップセット22件の削除・
    9セットの`k`置き換え以外の変更0件、`of`/`ja`/`en`/`sr`/`y`/`codeAlias`の変更0件
  - 新k配列1,544件全件について`imageIndex.json`上の画像存在を確認→1,544/1,544一致
  - 画像ディレクトリの実ファイル数とk配列件数を突合→9セット全件で一致（孤児残存なし）
  - `check-row-alignment.mjs`→疑いのあるセットなし、`detect-contaminated-en-names.py`
    →0件、`npx vitest run`→117件通過（既知の1ファイル問題を除く）、`npm run build`成功
- **積み残し（今回のスコープ外）**:
  1. **SIの非数値8件の正体不明**（上記参照）
  2. **S4a等30セットのうち残り21セット**は未着手（今回は該当10セットのみ。うち9セットを
     修復、1セット(SM2p)は健全と確認）
  3. SIの`ja`/`y`が空のままなのは今回の作業対象外（元々未設定）

### 対応（2026-08-31）: データ作業を一区切り、残作業の棚卸し

ユーザー指示により、cardData.json の網羅性向上（未収録セット追加・既存セットの
シークレット欠落パッチ・破損データ修復）を目的としたデータ作業はここで一区切りとする。
**残作業は「実際にそのカードを出品したい」と思ったときに個別対応する方針**とし、
再開時にこのセクションから必要なものだけを拾えるよう、保留項目を棚卸しして記録する。

#### 1. 既存セットのシークレット欠落: 残り46セット・1,138枚

`official-card-cache.json`の件数が`cardData.json`の`k`配列件数を上回るセットを
機械的に列挙すると46セット・合計1,138枚の差分が出る（下記コマンドで再計算可能）。
ただし**この数字は「実際に欠けている枚数」ではなく「生の件数差」**である点に注意。
official-card-cache.jsonには同一カードに対する重複cardID（別掲載・再スキャン等）や
番号非印字の基本エネルギーが含まれるため、実際に検証すると差分の大部分が
「実は既に完全」というケースになる（本セッションで実施した26セットのパッチ作業でも、
26セット中20セットは検証の結果「追加できる番号なし」だった）。**このリストは
「調査が必要な候補」であって「確定した不足リスト」ではない**。

再計算コマンド:
```js
const cache = require("./scripts/official-card-cache.json");
const data = require("./src/cardData.json");
const byCode = new Map(data.map(s=>[s.c,s]));
for (const [code, arr] of Object.entries(cache.setMap)) {
  const s = byCode.get(code);
  if (!s) continue;
  const deficit = arr.length - s.k.length;
  if (deficit > 0) console.log(code, s.k.length, "/", arr.length, "deficit=", deficit);
}
```

**(a) 別方式が必要（このリストの対象外・要位置マッチングスクリプト）**:

| setCode | 現在の枚数/cache | 差分 |
|---|---|---|
| SV-P | 94/322 | 228 |
| S-P | 151/334 | 183 |

SV-P・S-Pはプロモカードで、`details.php`にカード番号（"NNN/NNN"）が印字されていない
（既存の`scrape-promo-images.mjs`が使う位置マッチング方式で取得済みの94枚・151枚が
現状の全て）。本セクションの`scrape-missing-sets.mjs`/`patch-existing-sets.mjs`/
`rebuild-corrupted-sets.mjs`（いずれもRoute A=details.phpの番号印字が前提）では
対応できない。対応する場合は`scrape-promo-images.mjs`と同じ位置マッチング方式の
拡張が必要（下記(c)の無番号プロモ群とも設計を共通化できる可能性がある）。

**(b) 本セッションで検証済み・完全と確認済み（このリストに出てくるが対応不要）**:

以下23セットは、本セッションで`patch-existing-sets.mjs`（実際にfetchして検証、
「追加できる番号なし」を確認）または`rebuild-corrupted-sets.mjs`（truthとの全数突合で
完全一致を確認）のいずれかで**既に個別検証済み**。表に出る差分は同一カードへの
重複cardID・番号非印字の基本エネルギー等による見かけ上のもので、実データの欠落ではない。
**再調査は不要**:

SI(144) S12a(97) S11a(46) S9a(45) SVI(36) SML(18) XYE(10) SMA(9) MC(8) CP4(7)
20th(7) SMD(6) SM1p(5) SM2p(4) WCS23(2) S8b(2) XYD(2) XYC(2) XYA(2) XYH(1)
XYB(1) L2-Sb(1) L2-Sh(1)

**(c) 未検証・個別対応が必要（21セット・271枚）**: 元は「トレーナーズカードが0件」の
機械判定でS4a型破損の疑いありとしていた30セットのうち、今回投入した10セット
（S4a, SI, S8b, S11a, SV6a, S10D, S5a, S6H, S7D, SM2p）を除く残り。**破損か
単純なシークレット欠落かも含めて未調査**。対応する際は、まず`official-card-cache.json`/
`details.php`との全数照合（サンプリングではなく）で状態を確認してから、
「シークレット追加のみで済むか」「truthでの全件置き換えが必要か」を個別判定すること
（本セッションの手順をそのまま踏襲すればよい。手順の詳細は上記
「対応（2026-08-31）: S4a型データ破損の10候補セットを調査・9セットを全件取り直し」参照）。

| setCode | 現在の枚数/cache | 差分 |
|---|---|---|
| S10a | 71/148 | 77 |
| S11 | 100/127 | 27 |
| S10b | 79/101 | 22 |
| S10P | 67/88 | 21 |
| SH | 59/78 | 19 |
| S6a | 69/87 | 18 |
| S8 | 100/115 | 15 |
| S6K | 70/83 | 13 |
| S7R | 67/79 | 12 |
| S5R | 70/81 | 11 |
| S5I | 70/81 | 11 |
| S4 | 100/111 | 11 |
| SV6 | 130/133 | 3 |
| SV5M | 97/100 | 3 |
| SVC | 22/24 | 2 |
| SVEM | 21/22 | 1 |
| SVEL | 21/22 | 1 |
| SVAM | 25/26 | 1 |
| SVAL | 23/24 | 1 |
| SVAW | 25/26 | 1 |
| S8a | 33/34 | 1 |

うちSVAL/SVAM/SVAW/SVC/SVEL/SVEMは構築済み半デッキ系の型番のみのセットコードで、
過去にSVAL/SVAM/SVAW/SVHK/SVHMで発見された「行ズレによる全体空欄化」
（2026-08-03対応、詳細は該当セクション参照）と同系統の可能性がある。SVHK/SVHMは
本セッションの26セットパッチで正常に処理できた実績があるため、SVAL/SVAM/SVAW/SVC/
SVEL/SVEMも同様の可能性はあるが未確認。

#### 2. SIの非数値ローカルID 8件（正体不明、除外済み）

上記「対応（2026-08-31）: S4a型データ破損の10候補セットを調査・9セットを全件取り直し」
参照。旧cardDataのSIに存在した"DAR"/"FIG"/"FIR"/"GRA"/"LIG"/"MET"/"PSY"/"WAT"という
非数値ローカルID8件（カード名が特殊エネルギーらしくないポケモン名になっていた）は、
official-card-cache.jsonの563件を全数照合しても対応するカードが見つからず、正しい
内容を検証できなかったため復元せず除外した。正体（実在するカードなのか誤混入データ
なのか）は未解明。対応する場合はTCGdex側の生成元データや実物カードの現物確認など、
このプロジェクトの通常の取得経路（official-card-cache.json + details.php）以外の
情報源が必要になる見込み。

#### 3. その他の保留項目（既存記載の再掲・集約）

- **S12aの251-258番**（基本エネルギーのシークレット8枚）: Web/リテール検索で存在は
  確認できたが、official-card-cache.jsonのスキャン・cardId近傍探索のいずれからも
  見つけられず未回収。2026-08-31時点でも251-258番は`k`配列に存在しないことを再確認済み。
- **VS1/web1のpcg-search.com画像取得URL**: 既知の`/img/vs/vs0{NNNN}.png`パターンで
  既知の正しい番号ですら404になる状態。サイト構造が変わった可能性があり未調査。
- **未着手・要再分類リスト**（無番号プロモ群・番号衝突セット、詳細は本ファイル内の
  該当リスト参照）: `XYP`(456) `SMP`(443) `BWP`(246) `DPP`(175) `XY-P`(14) `DPtP`(65)
  `LP`(92) `M-P`残り91枚 `ENE`(85)は無番号プロモで位置マッチング方式が必要
  （上記1(a)のSV-P/S-Pと合わせて設計を検討すること）。`SA`(122) `MG`(34)は複数商品が
  番号帯を共有し1セットとして一意特定できない番号衝突セット（カード単位のサブセット
  識別の仕組みが必要）。

**再開時の使い方**: 出品したいカードのセットコードが上記1(c)の21セット・2の項目・
3の項目のいずれかに該当する場合のみ、そのセット単体を`scripts/rebuild-corrupted-sets.mjs`
の`TARGET_CODES`（または`scripts/patch-existing-sets.mjs`の`TARGET_PATCHES`）に追加して
個別に検証・取得すればよい。1(b)（既に完全と確認済みの23セット）や1(a)（SV-P/S-P、
別方式）はこのリストから除外して考えてよい。

---

## ファイル構成

```
src/
  App.jsx              # メインUIコンポーネント
  cardData.json        # 全セット・全カードのマスターデータ
  imageIndex.json      # セット/localId → 画像パスのマップ（自動生成）

scripts/
  filename-utils.mjs             # ファイル名ビルド・解析ユーティリティ（共通）
  rename-images.mjs              # 既存画像を新ファイル名形式にリネーム（冪等）
  build-image-index.mjs          # imageIndex.json を再生成
  scrape-official-images.mjs     # 公式サイトから日本語JPGを取得（S/SV/M系）
  scrape-pcg-search.mjs          # pcg-search.com からpngを取得（旧シリーズ）
  scrape-pmcg-gym-supplement.mjs # PMCG5/6 トレーナーカード補完（手動マッピング）
  scrape-missing-sv.mjs          # S/SV系 括弧付き名前・重複名マッチング補完
  fix-card-names.mjs             # cardData.json の誤った日本語名を修正しファイルもリネーム
  official-card-cache.json       # pokemon-card.com APIキャッシュ（2026-07-04生成）
  card-list.json                 # 全カードフラットリスト [{serie, set, local, ja}]
  pcg-search-cache/              # PMCG5/6 用 name→siteNum キャッシュ

public/
  cards/               # ダウンロード済み画像（{sr}/{set}/{jaName}_{setCode}-{localId}／{total}_{rarity}.ext）
```

---

## 画像ファイル名形式

`{jaName}_{setCode}-{localId}／{total}_{rarity}.ext`

- `／` は全角スラッシュ (U+FF0F)。Windows ではファイル名に `/` が使えないため代替
- `total` は各セットの最大数値 localId（`computeSetTotal` 関数で計算）
- レアリティなしの場合は `_{rarity}` を省略

例:
```
フシギダネ_SV1S-001／198_C.jpg
サンドリュー_PMCG5-043／96_C.png
草のエネルギー_PMCG1-097／102.png
```

---

## 画像ソースと URL パターン

### 公式サイト（S/SV/M系）
`https://www.pokemon-card.com/assets/images/card_images/large/{set}/{filename}.jpg`

### pcg-search.com（旧シリーズ）

| セット | URL パターン |
|--------|-------------|
| PMCG1 通常 | `/img/1st/1st1{NNN}.png` |
| PMCG1 エネルギー(097-102) | `/img/1st/1st1s001.png` 〜 `1st1s006.png` |
| PMCG2-4 | `/img/1st/1st{2-4}{NNN}.png` |
| PMCG5 | `/img/1st/1stgym1{NNN}.png` |
| PMCG6 | `/img/1st/1stgym2{NNN}.png` |
| neo1-4 | `/img/neo/neo{1-4}{NNN}.png` |
| E1-E5 | `/img/e/e{1-5}{NNN}.png` |
| PCG1-9 | `/img/pcg/pcg{1-9}{NNN}.png` |
| VS1 通常 | `/img/vs/vs0{NNNN}.png`（4桁、142まで）|
| VS1 基本エネルギー | `/img/vs/vs0en1.png` 〜 `vs0en6.png` |
| VS1 特殊エネルギー | `/img/vs/vs0en7.png`(悪) `vs0en8.png`(鋼) `vs0en9.png`(レインボー) |
| web1 | `/img/web/web{NNNN}.png`（4桁）|

---

## imageIndex.json について

- `node scripts/build-image-index.mjs` で再生成
- 優先度: `.jpg` > `.png`（webp は完全除外）
- キー形式: `"{setCode}/{localId}"` → 値: `"/cards/{sr}/{set}/{jaName}_{setCode}-{localId}／{total}_{rarity}.jpg"` など

---

## 画像を一から取得し直す場合

```bash
npm run images
```

`scripts/scrape-all.mjs` が以下を順番に実行します:
1. 既存ファイルを新ファイル名形式にリネーム（`rename-images.mjs`、冪等）
2. 公式サイト (S/SV/M 系 JPG)
3. pcg-search.com（旧シリーズ PNG）
4. PMCG5/6 トレーナーカード補完
5. S/SV 括弧付き名前・重複カード補完
6. 特殊 URL エネルギー9枚（PMCG1スターター + VS1特殊エネルギー）
7. imageIndex.json 再生成

`npm run images` に含まれないスクリプト（手動実行が必要）:
- `scrape-promo-images.mjs` — S-P / SV-P プロモの位置マッチング取得
- `scrape-old-images.mjs` — 英語版 TCGdex CDN からの代替取得。**日本語カードの出品に英語アートワークは不適切なため通常は使わないこと**

---

## eBay 出品文ノウハウ（`src/App.jsx` の `buildSingleDesc` / `buildPackDesc` に反映済み）

- シングル: 状態表記（NM/LP/MP/HP）、sleeve + toploader 梱包を記載
- パック/BOX: 「Factory Sealed」「Never weighed」、「開封結果は運＝返品不可」の自衛文
- 共通: 日本からの発送日数・追跡付き、タイトルは 80 文字以内

---

## ユーザー情報

- 名前: よういち
- 販売: eBay で日本語版ポケカを海外向けに販売（仕入れ: メルカリ / ヤフオク / 駿河屋）
- 日本語でやり取りしている。回答は簡潔さを好む
